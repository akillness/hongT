# HongT — Cinder Court (Unity) repository operating rules

Unity 재구현 프로젝트의 저장소 계약. `AGENTS.md`는 이 파일을 가리킨다. 계약은
하나다. 소스 프로젝트(`~/orca/Abyssal-Surge`)의 규칙을 이식하되, 엔진 관점은
이 저장소에 맞게 **Unity + WebGL**로 대체한다.

---

## 0. Lineage

- 원작: Abyssal Lantern — Hold the Cinder Court (Three.js/Canvas 2.5D,
  NAN 2026 제출본). 소스 저장소는 **읽기 전용 자산·규칙 소스**다.
- 이 저장소: 동일 게임의 Unity 6000.5.6f1 / URP / WebGL 재구현.
- 배포 계약: <https://akillness.github.io/hongT> (GitHub Pages, gh-pages 브랜치).
  모든 런타임 URL은 상대 경로만 사용한다. 루트 절대 경로(`/Build`, `/assets`)
  금지.

## 1. Engine perspective: Unity + WebGL only

- Unity 6000.5.6f1, URP 17.5, WebGL 타깃. 에디터 자동화는 배치모드
  (`Unity -batchmode -executeMethod ...`)로만 수행하고 명령을 기록한다.
- **Three.js/DOM 가이드는 여기 적용하지 않는다.** 원작 코드는 시맨틱(수치,
  규칙) 추출용 참조다.
- 시뮬레이션은 결정론적 고정스텝 60 Hz 순수 C# (`CinderCourt.Sim`,
  UnityEngine 참조 금지). 프레젠테이션(`CinderCourt.View`)은 심 상태를 읽기만
  하고 되쓰지 않는다. 이 경계는 원작의 하드 인바리언트를 계승한다.
- **심 인터페이스는 동결이다. 새 읽기 표면은 추가형으로 만든다.**
  `CinderSim`이 구현하는 것은 6개: `ICinderSim` · `ICampaignSnapshot` ·
  `IHackSnapshot` · `IRunPreparationSnapshot` · `IGrowthChoiceSnapshot` ·
  `IDerivedStatSnapshot`. 뒤 셋은 전부 "앞의 것을 개정하지 않기 위해"
  존재하며 각자 파일 머리에 그 이유를 적어 두었다.
  - 노출하려는 값이 **이미 다른 심에 있는지 먼저 확인한다.** cycle-6의
    첫 스펙은 `GrowthAttack/Vitality/Swiftness`를 요구했는데 셋 다
    `IGrowthChoiceSnapshot:40-42`에 있었다 — 추가했다면 한 사실에 두 출처
    (§4i)가 됐을 것이다.
  - 이미 계산된 필드를 프로퍼티로 노출하는 것은 **골든을 움직이지 않는다.**
    산술이 늘지 않으므로. 새 식을 심에 넣는 것과는 다른 일이다.
- WebGL 제약: compute/threads 금지, 텍스처 ≤1024, 캐릭터 ≤25k tri,
  총 빌드 ≤120 MB. gzip + decompression fallback.

## 2. Numeric contract (제출문서 §2.3 이식)

고정스텝 `1/60`. 아레나 1536×1024, 중심 (768,604), 반경 520×270.
워든 HP 100 / 이동 218 u·s⁻¹ / 공격 58 / 사거리 160 / 쿨 0.48 s.
적 HP 58 / 사거리 76 / 쿨 1.22 s. 동시 상한 20.
기름 최대 100, +7/s, +6/처치. Nova 45 기름·6.5 s·반경 250·피해 96.
Ward 30 기름·9 s·지속 3 s. 드롭 shard +18 HP / flask +35 기름 /
relic +250 점수, 수명 12 s, 자력 78. 아이소 거리 `hypot(dx, dy*1.42)`,
전방 판정 `dx*facing ≥ -18`. **숫자는 게이트다. 형용사는 게이트를 못 넘는다.**

## 3. Asset generation: fixed tool per asset class

| Asset class | Tool | Invocation |
|---|---|---|
| 컨셉/텍스처/아틀라스 | god-tibo-imagen | `gti --prompt "..." --input <ref> --output <path>` (`--dry-run` 먼저) |
| 2D 스프라이트/시트 | perfectpixel | `ppgen -provider god-tibo-imagen -desc "..." -json` |
| 3D 리스킨/애니메이션 | Blender 5.x headless | `blender -b -P tools/blender/<script>.py -- ...` |
| SFX | ElevenLabs sound-generation API | `python3 tools/audio/gen_sfx.py` (키: env `ELEVENLABS_API_KEY`, 커밋 금지) |

- 캐릭터 스키닝 계약: 소스 메시(Abyssal-Surge 모션 라이브러리)를
  **mixamo 표준 휴머노이드 스켈레톤에 자동 웨이트로 재바인딩**하고 FBX로
  내보낸 뒤 Unity Humanoid 아바타로 리타겟한다. 원작의 절차적 영역분할
  스키닝(메시 파손 원인)은 재사용 금지.
- 생성 산출물은 `docs/provenance/`에 프롬프트·소스·도구를 기록한다.
- 액션 라이브러리(11종): `idle move run hit bighit attack critical avoid
  defence die show`. idle/move/run 루프, 나머지 원샷.

## 4. Workspace and evidence

- Unity 소스(`Assets/`, `Packages/`, `ProjectSettings/`, `tools/`, `docs/`)는
  일반 소스 트리로 편집한다. `_workspace/current/`는 **증거·레인 아티팩트
  전용**(intake/design/engineering/qa/ops/pm/production — 리포트, 측정, 협상
  기록, 태스크 매니페스트)이며, 이전 사이클은 `_workspace/archive/<run-id>/`로
  `git mv` 후 읽기 전용. 단 `engineering/icons/`·`engineering/reskin/`은
  라이브 도구(tools/) 출력 경로라 아카이브하지 않는다.
- `[OBSERVED]` / `[INFERENCE]` / `[TARGET]` 표기를 유지한다. 측정 없는 주장
  금지, 정확한 저장소 상대 경로 인용.
- 검증 명령: EditMode 테스트 `bash tools/unity_batch.sh tests`, 컴파일 게이트
  `bash tools/unity_batch.sh import-only`(~15초), WebGL 빌드
  `bash tools/unity_batch.sh build`, 로컬 서빙 `python3 -m http.server`.
- **편집 묶음마다 컴파일 게이트를 돌린다.** 심 레인은 dotnet(1.7초), 뷰 레인은
  `import-only`(~15초). cycle-3에서 뷰 레인이 이걸 건너뛰어 사용자가 먼저
  플레이 에러를 발견했고, cycle-4에서 또 건너뛰어 `GetInstanceID()`
  (Unity 6000.5에서 CS0618이 아니라 **CS0619 error**)로 다른 레인의 테스트
  런을 폐기시켰다. **두 사이클 연속 같은 실수다.**
  일반화: 값싼 확인이 존재하는 곳에서 추론은 비용 절감이 아니라 부채다.
- 로컬 툴체인(머신 필수 — cycle-2에서 전부 재발견함, 반복 금지):
  ① Unity 6000.5.6f1 + WebGL 모듈(/Applications/Unity/Hub/Editor,
  `tools/unity_batch.sh`가 기본 경로로 인식) ② git-lfs(`git lfs pull` —
  FBX/폰트가 포인터면 휴머노이드·프롭·폰트 테스트가 오해를 부르는 실패)
  ③ `~/Library/Fonts/NanumBarunGothic.otf`(`tools/gen_hud_font.sh` 소스).
- **Unity 배치모드는 프로젝트를 잠근다.** 병렬 세션은 실행 순서를 조율하고,
  락 충돌은 XML 없이 EXIT=1로 조용히 끝나므로 결과 파일 존재를 먼저 확인한다.
- 다이제스트 회귀는 **런타임 내 비교만 유효**: 스탠드얼론 dotnet 다이제스트와
  Unity 다이제스트는 float 하위비트가 다르다(정수 필드는 동일). 배포 진실은
  Unity 골든(DungeonGoldenDigestTests) — 스탠드얼론은 pre/post 추가성 증명
  전용이다.

## 4b. 폰트: 두 규칙이 다르면 그 차집합이 사각지대다

WebGL은 OS 폴백이 없다. 서브셋에 없는 글자는 **두부로 렌더된다.**

- 한글 UI 문자열이나 **비ASCII 기호**를 추가하면 `bash tools/gen_hud_font.sh`
  재실행 + `Assets/Resources/Fonts/HudKorean.otf` 동반 커밋. 주석의 한글도
  수집 대상이다(의도된 싸구려 상위집합).
- **소스 폰트에 없는 글자는 재생성으로 못 닫는다.** NanumBarunGothic에
  `·`(U+00B7)와 `−`(U+2212)가 **없다** — cycle-4에 둘 다 가시 문자열에서
  발견됐고 각각 `•`(U+2022)·ASCII `-`로 치환했다. 새 기호를 쓰기 전에
  소스 폰트 cmap을 확인하고, 없으면 치환한다. 생성기가 exit 1로 알려준다.
- **"소스에 있다"는 어느 cmap을 읽었느냐에 달렸다.** cycle-7 머지에서 내가 이
  규칙을 "틀렸다"고 판정할 뻔했다. 모든 서브테이블을 합집합한 프로브가
  U+00B7을 **present**로 보고했기 때문이다. 실제로는:

  | 서브테이블 | 엔트리 | U+00B7 |
  |---|---|---|
  | platform 0 / enc 3 / format 4 | 18,665 | 없음 |
  | platform 1 / enc 0 / format 6 (Mac 레거시) | **189** | **있음** |
  | platform 3 / enc 1 / format 4 | 18,665 | 없음 |

  `fontTools.subset`과 `getBestCmap()`은 **유니코드 서브테이블만** 읽는다.
  그래서 그 글자는 파일 안에 존재하면서 서브셋에 들어갈 수 없고 두부로 뜬다.
  **검사는 반드시 `getBestCmap()`으로 한다** — 합집합 프로브는 툴체인이 하지
  않는 질문에 답한다.
- **main도 이걸 밟았다.** cycle-7 머지 시점에 origin/main은 U+00B7이 없는
  폰트를 배포하면서 그 글자를 쓰는 가시 문자열 5개를 갖고 있었다 — 난이도
  배지, 목표 2줄, 동료 명령 토스트. 전부 `•`로 치환했다. 규칙이 있어도
  레인이 갈리면 다시 들어온다: 머지에서 폰트를 재생성하고 exit 코드를 봐라.
- **검사기가 대상보다 좁으면 그 차집합은 영원히 사각이다.**
  `FontCoverageTests`가 한글만 걷고 생성기는 따옴표 전체를 걷던 시절, 구두점
  탈락은 구조적으로 안 보였다. 지금은 둘 다 따옴표 안 비ASCII 전수다.
  **어느 한쪽을 바꾸면 다른 쪽도 바꾼다.**
- 더 나쁜 형태 하나: 생성기 정규식 `"([^"\\]*)"`가 백슬래시 포함 리터럴을
  건너뛰어 짝짓기가 어긋나면 **문자열이 통째로 매치 틈에 빠진다.** 그 상태로
  커버리지 검사는 자기 charset과 폰트를 비교하므로 `coverage: FULL`을 준다.
  수집 실패가 검사 실패가 아니라 **검사 통과**로 나타난다. 그래서 규칙이
  union이다 — 하나를 지우지 마라.

## 4c. 뷰 레인은 화면을 열어봐야 검증된 것이다

cycle-3: EditMode 319/319 초록·골든 무이동·빌드 errors 0인 상태에서 게임을
열었더니 **7건이 더 나왔다.** 전부 뷰 레인이고 전부 사람이 화면을 봐야 보이는
것이었다(안 그려짐, 부모가 캔버스 밖, 라벨 겹침, 문자열 조합 오류, 심은 옳고
뷰만 틀림).

- **뷰 변경이 있는 사이클은 브라우저 스모크가 게이트 항목이다.** 선택이 아니다.
- 절차: `bash tools/unity_batch.sh build` → `cd build-webgl && python3 -m
  http.server` → 브라우저. 진행 상태가 필요하면 localStorage 키
  `abyssal-lantern:unity:campaign`에 세이브 JSON을 심고 리로드한다
  (형식은 `CampaignStore.Save`).
- 캔버스 좌표 계산은 `CanvasScaler` 공식 그대로:
  `scale = 2^(log2(w/1280)*(1-match) + log2(h/720)*match)`,
  `match`는 세로 0.35 / 가로 0.5. 눈대중 좌표로 클릭하면 빗나간다.
- **rect 겹침 0과 글리프 넘침 0은 다른 명제다.** 레이아웃 테스트는 전자만
  본다. 후자는 육안이다.

## 4d. 감사와 래칫: 상태가 컨트롤을 숨기면 감사도 숨는다

`LobbyLayoutTests`의 터치 플로어 표는 **부채 표**다. 신규 위반은 결함,
이탈은 개선. cycle-3에서 이 표가 설계를 한 번 고쳤고(등급 버튼 15개 →
공유 행 1개), cycle-4에서 아코디언 헤더 4개를 잡았다.

- 감사는 **활성** 컨트롤만 잰다. 접기·탭 같은 상태 전환은 컨트롤을 감사에서
  제거한다. cycle-4의 아코디언이 26 → 13으로 좁혔고, 4폴드 상태 합집합으로
  고쳐 30이 됐다. **상태 UI를 추가하면 감사도 그 상태를 순회해야 한다.**
- 합집합 중복 제거는 **인스턴스 참조**로 한다. 경로 문자열은 형제 카드가
  공유하므로(`Group0/Body/Panel/Panel`) 진짜 컨트롤이 사라진다 — 넓히려던
  스윕이 한 겹 아래에서 다시 좁는다.
- **SANCTUM 탭은 아직 순회하지 않는다.** 선택된 탭만 측정된다. 의도적
  미해소이며 테스트에 `[KNOWN, DELIBERATELY UNCLOSED]`로 명시돼 있다.
- 빨간 래칫을 "예상대로"로 넘기지 마라. **판정보다 실측 표가 값진 경우가
  있다** — cycle-4의 감사 구멍은 실패 로그를 정독하다 발견됐다.

## 4e. 순수 뷰 개정의 계약 (AMENDMENT #8 유형)

상태를 화면에 옮기기만 하는 개정은 심 수치·저장 필드를 **0개** 추가한다.

- 골든 무이동은 PASS 조건이 아니라 **전제**다. 움직이면 순수 뷰가 아니다.
- 규칙을 복제하지 말고 **호출**한다: 해금은 `StageCatalog.IsUnlocked`,
  숙달은 `CampaignStore.MasteryComplete`, 해저드는
  `HazardOverride ?? CampaignStages 앵커`. cycle-3에서 뷰가 `CurrentPushSign`을
  자체 계산해 "심은 옳고 뷰만 틀린" 결함이 났다.
- **가격·임계를 뷰에 상수로 박지 마라.** `ProgressionGuide`가 유일 출처이고
  런타임에 읽어 비교한다. 미서명 협상(entry 6 각인 가격)이 움직여도 규칙이
  스스로 답을 낸다. 상수를 박으면 서명 때마다 게이트를 다시 연다.
- `CanvasGroup.alpha` 뒤의 텍스트는 **어떤 팔레트 색도 WCAG AA를 못 넘는다**
  (0.45에서 골드 3.07이 최선). 설명 텍스트는 `ignoreParentGroups`로 감쇠 밖에
  두고 InkDim(8.69:1)을 쓴다. 색 교체로는 해결되지 않는다.
- 도달 불가 분기를 `default:`로 삼키지 마라. cycle-4의 `HazardOf`가
  `SigilKind.None`을 제단으로 조용히 매핑했다 — 도달 불가의 근거가 타입이
  아니라 관례(모든 호출자가 SigilOrder를 돈다)였다.

## 4f. 단일 빌더 — 겹침의 유일한 원인 (cycle-5 전수 감사)

QA가 D2(사망 패널 26u 겹침) 유형을 전수 감사해 **추가 8건, 101,792u²**를
찾았다. 규칙이 나왔고 **예외가 0건**이다:

> 한 함수에서 만든 패널은 겹침 0.
> **두 곳 이상에서 만든 대역은 전부 겹침.**

- **패널 본체·라벨·버튼은 한 함수에서 만든다.** 나중에 다른 함수가 버튼을
  덧붙이면 그 함수는 선행 rect를 볼 수 없다. D2가 정확히 그것이었다 —
  `Build()`가 라벨을, `EnableCampaignUi()`가 버튼을 만들었다.
- **정적 y는 한 구성에서만 맞다.** 성장 판 y=150은 데스크톱 스킬행 기준이었고,
  터치가 그 행을 120u 올리자 81.6%가 묻혔다. 그리고 터치 가로는
  `ShowRotateHintIfPortrait`가 **유도하는 구성**이다. 바닥 스택은
  `ApplyBottomStackPlacement()`가 스피커 라인 기준으로 재배치한다.
- `Label()`은 y를 **부모 top-left에서 아래로** 잰다. 양수 y는 부모 **밖**이다
  (성장 판 제목이 판 위 16u에 떠 있었다).
- 새 UI를 얹기 전에 **그 좌표대를 먼저 비운다.** 겹친 자리에 얹으면 새 것도
  같이 묻힌다.

## 4g. 정지(timeScale 0)의 계약 (AMENDMENT #9)

콘솔이 0.2로 이 함정을 먼저 밟았다(`HudView` `ResetRunUi` 가드). **0은 정도가
아니라 종류가 다르다 — `Time.deltaTime == 0`이라 해제 애니메이션도 안 돈다.**

- `ApplyTimeScale` **최상단에서 조기 반환**한다. 스무딩 이즈백이 카드 아래에서
  1로 걸어 올라가면 안 된다.
- 해제 폴링은 `MonoBehaviour.Update`(무스케일 벽시계). 스케일 기반 타이머는
  영원히 안 돈다. 입력은 `InputAdapter`가 아니라 **디바이스에서 직접** 읽는다 —
  어댑터 래치는 얼어붙은 틱 루프가 소비한다.
- `ResetRunUi`가 **모든 종료 경로에서** 카드를 뜯는다. 콜백은 발화시키지
  않는다 — 못 읽은 레슨을 소비하면 안 된다.
- **확인 모달도 정지에 포함한다.** 결정하는 동안 죽으면 신중함을 처벌하는
  안전장치가 된다 (브라우저에서 실제로 그랬다).

## 4h. 세이브 비트 상한은 31이다, 32가 아니다

`CampaignStore.ExtractInt`는 `char.IsDigit` 루프다. **선행 `-`에서 0글자를 읽고
0을 반환한다.**

```
bit 30 -> "1073741824"  -> 1073741824   ok
bit 31 -> "-2147483648" ->          0   전체 소실
```

- 비트마스크 필드의 **usable ceiling은 31**. sign bit를 세우면 다음 로드에서
  필드 전체가 0이 되고 조용히 전부 초기화된다.
- 비트 인덱스는 **영구 고정**이다. 추가는 안전, **재정렬은 조용히 전부를 다시
  가르친다**.
- 신규 int 필드는 additive: 키가 없는 구 세이브는 0으로 로드되고 그것이
  정확한 사실이어야 한다 (v4 각인·v5 훈련·v6 안내 전부 이 관례).

## 4i. 두 정보원을 마주 세워라 (cycle-5, 같은 뿌리 2건)

이 사이클의 결함 둘이 같은 형태였다.

| 주체 | 정보원 A | 정보원 B | 결과 |
|---|---|---|---|
| director | 파일의 현재 줄 번호 | 내 기억("방금 봤다") | doc 주석을 코드로 덮어씀 |
| 테스터 | 파서 동작에서 역산한 천장 31 | 가드 리터럴 `bit < 32` | 같은 파일에 모순된 어서션 2개 |

- **앞선 편집이 줄을 밀었으면 재읽기 없이 다음 편집을 하지 마라.** 편집 도구가
  stale-tag를 거부하는 이유가 그것이고, "방금 봤으니 안 변했겠지"가 틀리는
  경우가 정확히 그때다. cycle-4·cycle-5 연속 발생 — 습관이 아니라 절차 문제다.
- **상한을 서술하는 주석을 달았으면 그 상한을 검사하는 어서션이 같은 계산을
  돌려야 한다.** 그러면 두 주장이 출처를 공유해서 갈라질 수 없다. 서술이 아니라
  라운드트립을 어서션한다.

## 4j. 안내(guidance)를 추가할 때

- 문안의 수치는 **상수를 보간**한다. 재입력 금지 — 밸런스가 움직이면 레슨이
  따라 움직여야 하고, 심에서 어긋난 레슨은 없느니만 못하다.
- 안내 큐는 **한 번에 한 장**만 뽑는다. 두 장이 겹치면 한 번의 키 입력으로
  둘 다 닫힌다.
- 레슨은 **행동 가능한 시점**에 준다. 패배 조건은 `GameOver`가 아니라 첫
  피격에(그때 `ResetRunUi`가 카드를 뜯는다), 스킬은 기름이 그 비용에 닿은
  첫 프레임에.
- **정지 횟수보다 거절 가능성이 변수다.** 서베이 실측: Returnal은 무정지인데
  과잉으로 혹평받았고 Into the Breach는 정지를 쓰는데 불만이 0이다.
- `FontCoverageTests`의 정규식 소스 스캔은 **런타임 보간 조립 문자열을 못
  본다**. 상수를 보간해 문안을 만들면 전용 `HasCharacter` 전수 검사를 붙여라.

## 4k. uGUI: sprite 없는 Filled 이미지는 fillAmount를 무시한다

`Image.Type.Filled`인데 `sprite == null`이면 `Image.OnPopulateMesh`가
`Graphic`의 것으로 폴백해 **풀 렉트 하나를 그리고 `type`·`fillAmount`를 아예
읽지 않는다.** 필드는 정확한 값을 갖고, 아무도 그것을 읽지 않는다.

cycle-5에 이 상태로 **게이지 6개**가 살아 있었다 — 체력·기름·경험치·보스HP·
추출·스킬 쿨다운. 사용자가 "게이지가 안 달고있다"고 제보해서 발견했고,
실측은 **체력 0인데 바 98% 채움**이었다.

- 게이지는 반드시 `AsFilled()`를 거친다 (1×1 흰 sprite 공유).
  `Image.Type.Filled`를 직접 쓰지 마라.
- **rect 감사는 이 결함을 구조적으로 못 잡는다.** 크기와 겹침은 재지만
  "그 값이 화면에 반영되는가"는 정의역 밖이다. EditMode에서 `fillAmount`를
  읽으면 정확한 값이 나온다 — 필드는 맞고 렌더만 틀렸다.
- 같은 계열: sprite 없는 Image는 흰 쿼드로 그려진다(코드 주석에 이미 있던 사실).
  `Filled`는 그 사실의 더 조용한 형태다.

## 4l. 스모크 경로가 한 모드에 편중되면 그 편중이 결함을 숨긴다

cycle-5의 스모크 13항목이 **전부 던전 경로**였다. 그 편중 하나가 두 결함을
동시에 숨겼다:

- 게이지 — 던전 진입 시 체력 100이라 꽉 찬 바가 정상으로 보였다.
  **체력이 줄어드는 것을 끝까지 본 적이 없다.**
- 프롤로그 출구 — 프롤로그를 한 번도 안 열었다. `캠페인으로`는
  `EnableCampaignUi`가 붙이는데 프롤로그는 그 메서드를 호출하지 않아
  **버튼이 생성조차 안 됐다.**

- **기능을 넓혔으면 검증 경로도 넓혀라.** 안내를 2/23 → 23/23으로 늘리면서
  스모크는 한 모드에 머물렀다.
- 실행 모드는 넷이다: **Dungeon / Prologue / Training / Arena.** 뷰 변경이
  공유 코드에 닿으면 최소 둘 이상에서 확인한다.
- **"스스로 끝난다"는 이기는 경로에만 참이다.** 이탈 경로를 모드별로 게이팅할
  때 지는 경로를 먼저 그려봐라 — cycle-5의 프롤로그 트랩이 정확히 그 누락이다.

## 4m. 테스트가 정답과 오답이 같아지는 좌표계에서 재면 통과는 의미가 없다

cycle-6에서 브라우저가 결함 3건을 잡았고 **셋 다 EditMode 통과 중이었다.**
구현이 맞아서가 아니라 **테스트가 두 구현을 구분할 수 없어서** 통과했다.

| 결함 | 정답과 오답이 같아지는 지점 |
|---|---|
| 게이지 (cycle-5) | `fillAmount` 필드를 읽으면 둘 다 맞음. 화면을 봐야 갈림 |
| 분해 `72.8 ← 72.8` | **아레나 심**에서는 `BaseDamage == SimConfig.PlayerDamage`. 캠페인에서만 갈림 |
| 안내 탭 238u 넘침 | 문자열을 읽으면 둘 다 맞음. 위치를 재야 갈림 |

- **테스트는 전제부터 어서션한다.** 캠페인 분해 테스트의 첫 줄은
  `Assert.That(derived.BaseDamage, Is.Not.EqualTo(SimConfig.PlayerDamage))` —
  이게 아니면 그 테스트는 아무것도 증명하지 못한다.
- **새 어서션은 변이로 증명한다.** 고친 것을 되돌려 RED를 확인하지 않으면
  그 테스트가 실패할 수 있는지 모른다. 3열→2열로 되돌리니 `승리 조건`·
  `패배 조건`이 패널 밖으로 나가며 RED였다 — 그래서 그 테스트는 진짜다.
- **`new CinderSim()`은 아레나다.** 메타·장비·성장이 전부 0이라 파생값과
  기본값이 같아진다. 캠페인 동작을 검증하려면 `HackConfig.TryDungeon`으로
  구성한 심을 써라.
- **글리프 메트릭은 캔버스 축척에 딸린다. 레이아웃과 테스트가 같은 축척에 있어야
  한다.** cycle-7 실측: 배치모드 기본 상태는 `pixelsPerUnit = 0.577`이고 그
  축척에서 `Text.preferredHeight`가 **모든 행에 8.66을 돌려준다** — 2줄짜리
  본문(rect 21u)도 8.66이다. 모든 문자열이 한 줄에 들어가는 축척이라 메트릭이
  텍스트에 의존하기를 멈춘다. `WorldSpaceCanvas()`를 **코덱스를 열기 전에**
  부르면(레이아웃이 그때 돈다) ppu 1.0에서 `rectH == prefH`가 정확히 맞는다.
  순서가 뒤집히면 높이 어서션이 `8.66 > rectH`가 되어 **아무것도 실패시킬 수
  없다.**
- **죽은 메트릭은 0만이 아니다. 모든 입력에 같은 값도 죽은 것이다.**
  `preferredHeight <= 0` 가드는 8.66을 통과시킨다. 전제 검사는 "값이 있나"가
  아니라 "값이 입력에 따라 변하나"를 물어야 한다.

## 4n. UI가 "뭐라고 하는지"와 "어디 있는지"는 다른 검사다

코덱스 테스트 9건이 전부 행의 **내용**을 물었고 **위치**는 아무도 묻지
않았다. 안내 탭이 패널 밖으로 238u 넘쳐 마지막 두 그룹이 잘린 채 통과했다.

- 스크롤 없는 패널에 목록을 넣으면 **월드 렉트로 담김을 어서션**한다.
  `GetWorldCorners`가 앵커·피벗 차이를 흡수하는 유일한 좌표계다.
- 열 분배는 **최소-깊이 우선**으로. "한 열이 N을 넘으면 다음 열로"는 한 번만
  바뀌고 나머지를 전부 한 열에 몰아넣는다 — 5그룹 중 4개가 그렇게 몰렸다.
- `anchoredPosition` 비교는 **같은 앵커끼리만** 유효하다. 데스크톱 스탯은
  우상단, 스택은 좌상단이라 로컬 y를 직접 비교하면 900u 떨어진 두 렉트가
  26u 겹친 것으로 보고된다.

## 4o. 런을 붙잡는 표면은 런이 끝날 때 반드시 내려간다

`포기` 버튼이 사망 패널 위에 살아남아 **이미 끝난 런을 포기하겠냐**고 물었다.
`PrepareRunUi`에서 4모드 전부 활성화하면서 내리는 경로를 안 만들었다.

- 켜는 곳을 추가했으면 **끄는 곳을 같은 커밋에서** 찾아라. 런 종료 지점은
  `SimEvents.GameOver`(`HudView.cs:2766`)와 스테이지 클리어(`:3113`) 둘이다.
- 기존 관례가 답을 갖고 있다: 런을 붙잡는 모든 표면이
  `SetTouchCombatControlsVisible(false)/(true)` 짝을 이룬다. 새 표면은 그
  목록에 합류하는 것이지 새 규칙을 만드는 게 아니다.
- **런을 붙잡는 표면끼리도 서로를 내린다.** cycle-7 브라우저: 이탈 모달이 떠
  있는 채로 `정보` 버튼이 눌려 코덱스가 모달 위에 열렸다. 모달의 raycast
  블로커는 480×340 자기 rect만 막고 좌측 스택은 그 밖이다. `OpenCodex`가
  `CloseAbandonModal()`을, `OpenAbandonModal()`이 `CloseCodex()`를 부른다.
- **`GuidancePaused`로 이걸 검사할 수 없다.** 그건 세 술어의 OR이라 코덱스만·
  모달만·둘 다에서 전부 true다. 정지는 언제나 옳았고 화면만 틀렸다 —
  AMENDMENT #9부터 cycle-7까지 안 잡힌 이유가 그것이다. 표면은 각자
  (`CodexOpenForTest` / `AbandonModalActiveForTest`) 읽어야 한다.
- **두 문장의 순서가 하중을 받으면 그 순서에 검사를 붙여라.**
  `CloseAbandonModal()`은 `SetTouchCombatControlsVisible(true)`를 부른다. 그래서
  `OpenCodex`는 그것을 먼저 부르고 `false`로 덮는다 — 최종값만 옳다. 두 줄을
  바꾸면 코덱스가 심을 얼린 채 조이스틱이 살아난다.

## 4p. 증분 생성기에 전수 매터를 물리지 마라

cycle-7에서 아이콘 2개를 추가하려고 `mat_icons.py`를 돌렸더니 **기존 20개가
전부 재작성**됐다. 19개는 픽셀 동일(LFS OID만 바뀜)이었지만 `ui-button`은
**256×96 → 256×106**으로 바뀌었다 — raw 마스터는 그대로인데.

- `gen_icons.sh`의 `run_one()`은 `[ -s "$out" ] && SKIP`으로 증분인데
  `mat_icons.py`에는 그 가드가 없었다. **생성기가 증분이면 매터도 증분이어야
  한다.**
- 지금은 이름을 지정해야 돈다: `python3 tools/icons/mat_icons.py ui-codex`.
  맨 호출은 exit 2로 거부한다. `--all`은 의도적 전수 재도출 전용.
- `ui-button`은 9-slice 플레이트이고 `spriteBorder (30,14,30,14)`가 **실제
  플레이트 픽셀을 인덱싱**한다. 높이가 10px 움직이면 모든 버튼의 모서리 아트가
  어긋난다. 커밋본은 1px 투명 여백을 갖고 있고 이 패스는 그걸 재현하지 못한다 —
  **같은 입력에 다른 출력이면 재매팅은 이득이 없고 손실만 있다.**
- 레인 밖 파일이 modified로 뜨면 픽셀을 비교해라. LFS는 diff가 포인터만
  보여주므로 `git show HEAD:<path> | git lfs smudge`로 실제 이미지를 꺼내
  비교한다.

## 4q. 만장일치로 통과한 결정에 테스트가 없다 (cycle-8 변이 스윕)

변이 스윕이 무방비 3건을 찾았고 **전부 만장일치 결정**이었다. 논쟁이 있었던
결정 중 무방비는 **0건**이다.

| 결정 | 성격 | 최초 방어 |
|---|---|---|
| 티어 삭제 | 디렉터 판정, 대안 기각 기록 | 있음 |
| 아이콘 크기 | QA가 스펙 기각, 재계산 | 있음 |
| 담김 불변조건 | QA 3건 추가 신고 | 있음 |
| 터치 기하 | 네 역할 독립 동의 | **없음** |
| 배지 | 중재 결과, 양측 합의 | **없음** |
| 레일 3개 동결 | 디자이너 제약, 이견 없음 | **없음** |

반대가 있으면 누군가 근거를 적어야 하고 그 근거가 단언이 된다. 모두가
동의하면 적을 것이 없어 보인다. **회의록에서 가장 매끄럽게 지나간 항목이
코드에서 가장 취약하다.**

- 변이 스윕은 **합의 항목부터** 겨눠라. cycle-7이 "무방비 지점 탐지기"라고
  적었고, 이번이 그 탐지기를 어디에 먼저 대야 하는지다.
- 결정 하나당 그것을 되돌리는 변이 하나. 되돌려도 초록이면 그 결정은
  코드에 없는 것이다 — 회의록에만 있다.

## 4r. 임계값보다 그것을 재는 조건을 먼저 명명하라 (cycle-8)

한 회의가 같은 92u 버튼에 대해 두 판정을 냈다:

```
기존 유지   1280 CSS로 측정 → 100.1px → PASS
신규 기각    501 CSS로 측정 →  39.2px → FAIL
```

최선 실척으로 기존을 재고 최악 실척으로 신규를 쟀다. 둘 다 계산은 정확했다.

- 근본 원인은 숫자가 아니라 **지원 하한이 문서 어디에도 없었다**는 것이다.
  하한이 없으면 모든 결정이 자기에게 편한 실척을 **정당하게** 고를 수 있다.
- 이 저장소의 지원 플로어는 **375×667 CSS (iPhone SE2)**다. 320·280은 명시적
  범위 밖 — 미달을 부채로 측정해 적되 통과를 주장하지 않는다.
- `build-webgl/index.html`은 두 밴드를 만든다. 어느 밴드에서 재는지 밝히지
  않은 px 수치는 무의미하다:
  ```
  밴드 A  CSS 폭 >= 501 && !((max-height: 500px) and (orientation: landscape))   aspect-locked 1280:853   최악 0.4261 px/u
  밴드 B  CSS 폭 <= 500 || ((max-height: 500px) and (orientation: landscape))    fill                     최악 0.4383 (지원 내)
  ```
- **단위(u)가 계약이고 px는 파생이다.** 배포 프레임 1.0884 px/u에서 px 단언은
  `44/1.0884 = 40.4u` 이상만 요구한다 — 92×92 계약이 84×44로 바뀌어도 통과한다.
  기하를 고정할 때는 u로 단언하라.

## 4s. 커버리지 아티팩트가 거짓말하면 그것을 검사할 상위 아티팩트가 없다

`LobbyLayoutTests`의 터치 플로어 부채 표가 **2사이클간 도달 불가 컨트롤을
감사됨으로 계상**했다. 구 배치는 패널을 화면 밖에 주차했을 뿐
**비활성화하지 않았고**, 감사는 `activeInHierarchy`로 거른다.

- 감사가 "활성"으로 거른다면 그것은 **존재** 검사이지 **도달** 검사가 아니다.
  도달을 주장하려면 캔버스 rect 안에 있는지를 재라.
- 항목 수가 그대로여도 그 항목들의 **참**이 바뀔 수 있다. diff가 무변경으로
  보이는 것이 가장 위험한 형태다 — 독스트링에 적어라.
- 이름이 거짓말하는 테스트는 없는 테스트보다 나쁘다. 이 저장소에서 **4번째**
  사례였다(§4m·§4n과 같은 뿌리). 접두사 조회는 이름이 유일할 때만 조회다:
  `GameObject.name` 같은 안정적 식별자로 찾고, 라벨은 별도 줄에서 단언하라.

## 4t. 앵커가 미측정량을 참조하면 그것은 앵커가 아니다 (cycle-9)

§4r은 "임계값보다 재는 조건을 먼저 명명하라"였다. cycle-9가 같은 규칙의
**한 단계 앞**에서 실패했다 — 조건이 아니라 **앵커 자체**가 측정 의존이었다.

설계자가 보상 크기를 `"제단 수급의 50%"`로 잡았다. 상대 표현이라 안전해
보이지만 제단 수급 = `floor(T/6) × 18`이고 **T(스테이지 길이)가 미측정**이다.
같은 문구가 T=180s에서 270, T=360s에서 540을 뜻한다.

- **상대 표현은 분모가 측정된 경우에만 안전하다.** "X의 50%"는 X가 계약값일
  때 앵커이고, X가 관측 대기값이면 **미결정 상태를 숫자처럼 보이게 하는 위장**이다.
- 고칠 때는 분모를 재는 게 아니라 **측정이 필요 없는 앵커로 바꾼다.**
  위 사례는 처치 기름(웨이브 표에서 결정)으로 교체했고, 그 결과 비율이
  `p × r / 6`으로 **스테이지에 무관**해졌다 — 상수 하나가 9스테이지를 덮는다.
  좋은 앵커는 측정을 없앨 뿐 아니라 **경우의 수도 없앤다.**
- 자기 규칙의 위반이 가장 늦게 발견된다. §4r을 쓴 레인이 §4r을 어겼고,
  잡은 것은 그 레인이 아니라 **수치를 실제로 계산해 본 다른 레인**이었다.

## 4u. 목표가 미달이면 목표를 재정의하기 전에 미달의 모양을 먼저 봐라 (cycle-9)

선택 압력 목표 ≥0.75가 9스테이지 전수에서 실패했다(실측 0.400~0.727).
반사적 대응은 "달성 가능한 값으로 낮춘다"이고 그것은 **미달의 모양을 지운다.**

```
0 cinder-span 0.400   ← 게임이 시작되는 곳
8 ash-march   0.727   ← 8클리어 후
```

- **분포가 균일한 미달과 기울어진 미달은 다른 결함이다.** 전자는 임계값이
  틀린 것이고, 후자는 **구조가 틀린 것**이다. 위 사례는 후자였다 — 그리고
  기울기의 방향이 하필 이 사이클이 고치려던 문제(첫 입장이 지루하다)와
  같았다. **처방이 병을 재생산하고 있었다.**
- 단일 임계를 진행 밴드로 바꾸는 것은 정당하지만, 그것으로 **구조 문제가
  해소됐다고 적으면 안 된다.** 밴드는 미달을 합법화할 뿐 기울기를 펴지 않는다.
  재정의와 함께 **잔존 구조 부채를 같은 문서에 등재**하라.
- 전수 산술이 가능한 지표는 플레이테스트 전에 재라. 위 9행은 코드 없이
  해저드 표만으로 나왔고, **설계 승인 전에 설계를 반증**했다.

## 4v. 범위를 모드 **이름**으로 적지 마라 — 플래그로 적어라 (cycle-9)

한 사이클이 범위를 "던전"이라 적고 진행했다. 사용자가 *"캠페인과 강화 두 개
던전"*, 이어 *"훈련에도 몹 나온다"*고 지적했고 **둘 다 맞았다.**

실측하니 진입점이 넷이고 성질이 전부 다르다:

```
           적    기믹   강화반영  스킬
Dungeon    웨이브  2~4    O        O
Prologue   4      0      X        X (봉인)
Training   0      2~4    O        O
Arena      무한    0      X        O
```

- **"훈련"이라는 한 단어가 두 모드를 가리키고 그 둘은 정반대다** — 프롤로그는
  적이 있고 강화가 없으며, 시련은 적이 없고 강화가 있다. 이름으로 범위를
  적으면 이 구분이 사라진다.
- 플래그는 1:1이 아니다. `_campaign = _dungeon`인 생성자가 있고, `_campaign`만
  참인 v0.1 호환 생성자도 있으며(테스트 전용), 강화는 `_dungeon || _training`
  이고 스킬 봉인은 `_prologue`다. **네 플래그가 서로 다른 조합으로 켜진다.**
- 기능이 "어디서 도는가"를 적을 때는 **게이트 조건을 그대로 인용**하라 —
  `_dungeon` 뒤, `_dungeon || _training` 뒤처럼. 이름은 요약이지 계약이 아니다.
- 격리를 주장하면 **게이트 제거 변이로 증명**하라(§4q). "안 움직였다"만 보이면
  기능이 아무 데도 안 붙었어도 같은 결과가 나온다.

## 4w. 심은 어셈블리째 컴파일해서 물어볼 수 있다 (cycle-9)

배치모드 락(직렬 실행, EXIT=1 무XML) 때문에 "심에게 물어보기"가 비싸다고
가정하고 있었다. 아니었다.

```
dotnet 콘솔 앱 + <Compile Include="Assets/Scripts/Sim/**/*.cs" />
```

`CinderCourt.Sim`은 `UnityEngine` 참조가 없다(asmdef가 강제). 그래서 **그대로
컴파일된다.** 4개 모드의 적·기믹·강화·스킬을 몇 초에 전수 측정했고, Unity를
한 번도 열지 않았다.

- 순수 심 질문(스폰 수·수치·결정론·분포)은 EditMode보다 이쪽이 빠르다.
  EditMode는 뷰가 얽힌 질문에 쓴다.
- 이것이 성립하는 이유는 **어셈블리 경계가 실재하기 때문**이다. 경계를 지키면
  나중에 이런 도구가 공짜로 생긴다 — 지금 안 보이는 이득이다.
- 하네스: `/tmp/simprobe`. 재생성 비용이 낮으니 사라지면 다시 만들면 된다.

## 5. Concurrent-session Git safety (원작 이식, 무수정)

- 다른 세션의 동시 편집을 가정한다. 편집 전과 커밋 직전 `git status --short`.
- 명시적 pathspec으로만 스테이징. `git add -A`/`git add .` 금지.
- 다른 세션의 변경 복원·폐기·강제 덮어쓰기 금지. 충돌 시 중단·기록·명시 해결.
- push 전 upstream fetch 후 `@{upstream}..HEAD` 전체 검사. force-push 금지.
- 파괴적 자산 작업 전 `git tag -f pre-<op>-<date>`.

## 6. Reporting

실제 확인한 것만 보고한다: 정확한 명령/아티팩트 경로와 관측 결과.
이월 증거·신규 증거·미해결 블로커·사람 판단 항목을 구분한다.

## 7. Knowledge wiki (llm-wiki)

- 전역 지식 위키는 `/Users/jangyoung/orca/workspaces/HongT/main/llm-wiki`
  (환경변수 `$JEO_WIKI_ROOT`) 하나뿐이며, 모든 세션·프로젝트가 공유한다.
  `.jeo/memory/`(프로젝트 한정 저장소)와는 별개이며 대체 불가.
- 지식/후속 질의를 받으면 답하기 전에 `llm-wiki/index.md`를 먼저 읽고, 이어서
  `llm-wiki/wiki/` 하위 관련 문서를 읽는다.
- 이 저장소에서 만든 지속적 결론(수치 계약 변경, 엔진/빌드 이슈 원인, 자산
  파이프라인 결정, 반복 재현되는 블로커·해법 등)은 작업 종료 전에
  `llm-wiki/wiki/`에 파일로 남긴다. 세션 임시 메모나 1회성 로그가 아니라, 다음
  세션·다른 프로젝트에서도 재사용 가능한 형태(원인·근거·저장소 상대 경로 포함)로
  기록한다.
- `[OBSERVED]` / `[INFERENCE]` / `[TARGET]` 표기(§4)를 위키 기록에도 동일하게
  적용한다.

## 4x. 두 점을 잇기 전에 그 둘이 무엇으로 다른지부터 확인하라 (cycle-9)

한 사이클이 유물 수입 실측 2점(r=17.7 / r=7.0)을 얻고, 그 차이를 **랭크**로
읽어 "수입이 mean rank에 비례한다"는 부트스트랩 곡선을 세웠다. 그 위에서
드롭율 시나리오 4개와 밴드 헤드룸을 런 단위로 계산했다. 정교했고 틀렸다.

전수 측정(6앵커 × 6랭크 × 2행동 = 72런):

| 요인 | 수입 분산 기여 |
|---|---:|
| **플레이 행동** | **85%** |
| 랭크 | 15% |

랭크-수입 상관은 음수(−0.46 / −0.13)였고, 두 앵커는 재현됐다 — 수거 18.55 ≈
17.7, 카이팅 6.78 ≈ 7.0. **두 점은 랭크의 양끝이 아니라 행동의 양끝이었다.**

- 두 점만 있을 때 사이를 무엇이 채우는지 정하는 것은 **가정**이다. 가정 위에
  곡선·시나리오·헤드룸을 쌓으면 **쌓을수록 가정이 안 보인다.**
- 축을 검증하는 비용은 대개 작다. 여기서는 랭크를 고정하고 행동만 바꾼 런
  6개면 끝났다 — 곡선을 세우기 전에 했어야 했다.
- 실측점이 둘이고 축 후보가 둘 이상이면 **곡선을 세우지 말고 브래킷으로
  보고하라.** 브래킷은 틀리지 않는다.

## 4y. 측정 프로토콜이 답을 고르게 두지 마라 (cycle-9)

`parity_sessions_band: [10, 20]`은 T5 도달 세션 수의 계약 밴드다. 같은 빌드·
같은 수치·같은 스킬에서 **봇의 이동 목적지 하나만 바꿨더니** 5.2와 14.2가
나왔다. 둘 다 정당한 플레이다.

- 단일 값 밴드는 **그 자리에 값이 하나라는 가정**을 품는다. 실측이 분포면
  그 밴드는 수치가 아니라 **형식**이 틀린 것이다.
- 그리고 더 나쁜 성질: **단일 봇으로 재면 측정자가 답을 고르게 된다.**
  §4r이 임계값에 대해 말한 것("재는 조건을 먼저 명명하라")을 여기서는
  측정 도구에 대해 말해야 한다.
- 행동 의존 지표는 **프로토콜을 계약에 넣어라** — `dual-bot` 같은 형태로.
  숫자만 계약하고 재는 법을 열어두면 다음 사이클이 자기에게 유리한 봇을
  고른다. 악의가 아니라 그냥 하나만 돌리기 때문이다.

## 4z. "측정 불가" 블로커는 도구의 한계인지 대상의 성질인지 구분하라 (cycle-9)

협상 항목 하나가 `blocked_on: "봇 0/6 클리어"`로 **세 사이클** 대기했다.
이번 사이클 봇은 클리어한다 — 5/6, 최상위 랭크에서 6/6.

차이는 **Nova·Ward를 매 틱 큐에 넣은 것**뿐이다. 기존 파일럿은 화력의 상당
부분을 쓰지 않고 있었다.

- 블로커에 "불가"라고 적을 때는 **무엇이 불가한지** 적어라. "봇이 못 깬다"는
  대상의 성질처럼 읽히지만 실제로는 그 봇의 한계였다.
- 이월된 블로커는 **매 사이클 도구 쪽을 한 번씩 의심하라.** 대상은 잘 안
  변하지만 도구는 쉽게 바뀐다.
