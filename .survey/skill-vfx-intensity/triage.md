# Triage
- Problem: 스킬 6종(균열 화살 / 묘지 파동 / 잿불 노바 / 공허 방패 / 랜턴 결계 / 질주)의 VFX가 이름이 약속하는 것을 시각적으로 이행하지 않는다. 현재 VFX 어휘는 사실상 **팽창하는 LineRenderer 링 하나**이고, 스킬 간 구분은 색상과 반경뿐이다 — "균열"에 균열이 없고, "묘지"에 묘지가 없고, "공허"에 공허가 없다. 강렬함(intensity)을 파티클 수로 올리려는 시도는 WebGL 예산과 아이소메트릭 가독성을 동시에 깨뜨린다.
- Audience: (a) 던전 모드 플레이어 — 4스킬 + 질주를 20마리 동시 교전에서 0.3 s 단위로 읽어야 함, (b) 프롤로그 플레이어 — 스킬 없이 기본 공격만, 전투 문법을 여기서 처음 배움, (c) 이 저장소의 View 레인 유지보수자 — `VfxDirector.cs` 1,574줄을 코드 생성 전용으로 유지해야 함(에셋 의존 0).
- Why now: 직전 사이클에서 애니메이션 리타이밍(`attack` 24% → 100%)과 HUD 게이지 렌더 버그를 고치면서 **전투 피드백 레이어가 이미 두 번 열렸다.** 남은 미이행 약속이 스킬 VFX다. 또한 `034836a` sigil 커밋이 업그레이드를 기믹에 붙이면서 스킬의 기계적 정체성이 강해졌는데, 시각적 정체성은 그대로다 — 이 격차가 지금 가장 크다.
