# Stage 2 Gate Review — v21

run-id `20260808-lobby-icon-rail` · game-production-director · AMENDMENT #11

결함 주도 Stage 2 재진입. 선행: `.omc/specs/deep-interview-lobby-icon-rail.md`
(R0+4R, 15%) → 5역할 설계 회의 `production/decision-log.md` D-1~D-12.

**개정 (D-12, 사용자 지시 4건).** 게이트 판정 후 사용자가 로비를 직접
구동하고 두 가지를 요구했다: 닫기 기능, 그리고 진입 시 닫힌 상태. 반복 끝에
최종 형태는 **활성 레일 아이콘 재클릭 = 닫기**, **기본값 = 닫힘**이다.

게이트 판정은 **유지된다** — 아래가 왜인지다.

| 게이트 | 영향 | 재측정 |
|---|---|---|
| G4 | 기하 무변경. 도달률·배경 노출 측정 그대로 유효 | 불요 |
| G1 | 신규 문자열 0개 (닫기 아이콘·라벨 전부 기각됨) | 불요 |
| G6 | 컨트롤 0개 추가, 부채 행 0개 추가 | 스위트 재실행만 |

기각된 두 형태는 각각 게이트를 건드렸을 것이다: 레일 하단 닫기 타일은
D-8(레일 3개 동결)에 직접 충돌했고, 패널 우상단 X는 56u = **24.5 CSS px**로
§4d 신규 미달 컨트롤 금지에 걸렸다(플로어 통과에 103.3u 필요 = 성소 폭의
1/4). 재클릭은 기존 103.3u 히트박스를 재사용하므로 **어느 쪽에도 압력을
주지 않는다** — 세 형태 중 유일하다.

---

## 판정 요약

| 게이트 | 판정 | 측정 | 증거 |
|---|---|---|---|
| G4 이펙트·애니 몰입 | **PASS** | 도달률 37.8% → **100%**, 지출 UI 0% → 100%, 배경 32u → **653u** | `qa/gate-measurements.md#g4` |
| G1 세계관 일관 서사 | **PASS** | 신규 라벨 3종 + 개명 1종, worldview 위반 0 | 아래 §G1 |
| G6 운영 계획 | **PASS** | EditMode **808/808**, 변이 **14/14**, 코드 경로 감소 | 아래 §G6 |
| G5 매출·밸런스 | **재측정 이월** | 도달률 0→100은 분모 변경, parity 밴드 실측 미완 | D-9, cycle-9 |
| G2·G3·G7·G8 | 이월 (무변경) | 심 수치·보상 밴드·코어루프·참신성 무접촉 | 순수 뷰 개정 |

S1 결함 0건. 미해결 블로커 0건. 변이 스윕이 무방비 3건(D-3·D-7·D-8)을 찾았고
**전부 사이클 내에 닫았다** — §변이 참조.

---

## G4 — 이펙트·애니메이션 몰입감 · PASS

### 제보가 실제로 무엇이었나

사용자는 "로비가 다 활성화되어 있다"는 UI 선호로 보고했다. 인테이크가
**배포 결함**으로 재분류했고, 회의(D-1)가 보고보다 더 나쁜 사실을 찾았다.

| 항목 | 이전 | 이후 |
|---|---|---|
| SORTIE 도달률 | 100% | 100% |
| SANCTUM 도달률 | **13.5%** | 100% |
| MAP 도달률 | **0%** | 100% |
| 성소 지출 UI 도달률 | **0%** | 100% |
| 3D 배경 노출 (배포 프레임) | 32u (2.7%) | **653u (55.5%)** |

지출 UI 0%가 이 사이클의 진짜 발견이다. 보이던 75.7u는 전부 패널 껍데기이고
`TabContent`는 116u 아래에서 시작한다 — **배포 빌드에 영구 강화 경제가 있고
구매 버튼 도달률이 0이었다.** 상단바 유물 카운터는 100% 가시였다. 잔고는 늘
보이고 문은 없었다.

### 왜 아무도 못 봤나

`SideBySideFloor = 1248`이고 배포 E_w는 **1176에 고정**이다
(`build-webgl/index.html:18-20`이 캔버스를 1280:853에 잠근다 — 종횡비가 배율
불변이라 640..3840 어떤 버퍼에서도 1176). 1176 < 1248이므로 **모든 플레이어가
항상 stacked**였고, side-by-side 분기는 에디터 Game 뷰에서만 나타난다.
사용자 스크린샷 1868×998 → E=1313.4 → 에디터다.

### 브라우저 실측 (§4c 게이트 항목) `[OBSERVED — 실제 배포 빌드 구동]`

WebGL 빌드 `result=Succeeded errors=0 warnings=1 size=88.2MB`
(`BuildWebGL-145924.log`). 로컬 서브, 헤드리스 크로뮴 1920×1080 창.

캔버스 실측 **CSS 1280×853, 1.0884 px/u** — 템플릿 종횡비 잠금이 예측대로
동작한다. 창은 1920인데 캔버스는 1280이다.

증거 `qa/lobby-rail-smoke/` 4장:

| # | 상태 | 결과 |
|---|---|---|
| 01 | 출정 (기본값) | 레일 3개, 출정 골드 활성, SORTIE 100% |
| 02 | 성소 | **구매 UI 전체 가시** — 탭 4개, 스탯 3행, + 버튼 3개, 하단 2버튼 |
| 03 | 지도 | 성좌 + "전체 지도"/"정비" 개명 확인 |
| 04 | 지도 3회 클릭 | 여전히 열림 — AC-4 라디오 확인 |

콘솔 에러 **0**, `pageerror` **0**.

### 미측정으로 남겼던 항목이 해소됐다

게이트 초안은 "워든이 열린 653u 안에 보이는지는 미측정"으로 적었다. 근거는
`LobbyStaging.cs:12-14`의 스팟이 `ViewWorld.ToWorld()` 3D 월드 좌표라 캔버스
u와 직접 비교할 수 없다는 것이었고, 그 판단은 옳았다 — 인터뷰 스펙 `:128-133`이
두 좌표계를 혼동한 채 "레일이 아무것도 가리지 않는다"고 단정했다.

**브라우저가 답했다: 워든이 보인다.** 01·02에서 우측에 보스가, 03·04에서
워든과 보스가 함께 렌더된다. 해석으로 도달할 수 없던 답을 화면이 줬다.

### 에디터 프레임 역행 (출하 대상 외)

패널이 오른쪽 115.3u 이동하므로 E=1313 에디터 프레임에서는 가림이 **늘어난다**.
그 프레임에서 캐릭터는 u[401,555]에 있었고 SANCTUM은 이제 531.3에서 끝난다.
D-2가 에디터를 출하 대상에서 제외했고, 위 실측이 배포 프레임에서는 문제가
아님을 확인했다.

## G1 — 세계관 내 일관 서사 · PASS

| 문자열 | 근거 |
|---|---|
| 성소 | `Eyebrow("SANCTUM","성소 정비")` |
| 출정 | `Eyebrow("SORTIE","출정")` |
| 지도 | `Eyebrow("CAMPAIGN","심연 지도")` |
| 전체 지도 (개명) | "지도"는 세계관 단어, 조합만 신규 |

D-5가 원안 "정보"를 기각했다. 세 라벨 중 유일하게 세계관 밖 단어였고 그것이
여는 패널 제목이 "성소 정비"였다 — 라벨 문제를 고치면서 새 라벨 불일치를
만들 뻔했다. 비용 0.

## G6 — 게임운영 계획 · PASS

| 항목 | 값 |
|---|---|
| EditMode | **808/808**, 실패 0 (`test-results-145644.xml`) |
| 변이 검출 | **14/14** (첫 스윕 11/14 → 무방비 3건 닫음) |
| WebGL | `result=Succeeded errors=0 warnings=1` 88.2MB (`BuildWebGL-145924.log`) |
| 브라우저 스모크 | 4상태, 콘솔 에러 0, `pageerror` 0 (`qa/lobby-rail-smoke/`) |
| 컴파일 게이트 | 편집 묶음마다, 마지막 errors 0 |
| 삭제된 코드 경로 | `SideBySideFloor`, `_stacked`, `StackedForTest`, 패널 2분기, 맵 2분기 |
| perf 예산 | 재측정 불요 — 패널 2/3이 비활성, 신규 per-frame 작업 0 |
| 아이콘 파이프라인 | 명명 스템 호출, 기존 20개 무변경 (§4p 준수) |

---

## 변이 증명 14/14 — 그리고 스윕이 찾은 패턴

| 변이 | 실패 | 첫 검출자 |
|---|---|---|
| M-A stacked 복원 (**배포 결함 그 자체**) | 4 | `CampaignMapActions_AreDrawnInside...` |
| M-B 맵을 옛 상수 432로 | 2 | `LobbyPanels_HoldTheirPinnedGeometry...` |
| M-C 라디오 삭제 (3패널 동시) | 2 | `AccordionHeaders_CoverNoExistingControl...` |
| M-D SelectRail을 토글로 | 3 | `CampaignMapActions_AreDrawnInside...` |
| M-E RailIcon 103.3 → 90.2 (스펙 원안) | 2 | `LobbyPanels_HoldTheirPinnedGeometry...` |
| M-F Hide가 레일 리셋 안 함 | 1 | `RailSelection_KeepsExactlyOnePanelActive` |
| M-G 기본값 출정 → 성소 | 3 | `ArmedPactSortie_DoublesOnly...` |
| M-H MAP 라벨 "지도" 복귀 | 1 | `CampaignMapActions_AreDrawnInside...` |
| M-I 터치 레이아웃 E_w 분기 복귀 (D-3) | 1 | `SortieActions_HoldTheirTouchGeometry...` † |
| M-J 레일이 PanelLeft에서 분리 | 3 | `AccordionHeaders_CoverNoExistingControl...` |
| M-K 티어 분기 부활 (D-2) | 1 | `LobbyPanels_HoldTheirPinnedGeometry...` |
| M-L 레일에 4번째 항목 추가 (D-8) | 1 | `Rail_CarriesExactlyThreeDestinations` † |
| M-M 레일 배지 상시 꺼짐 (D-7) | 1 | `SanctumRailBadge_MirrorsTheTabDots...` † |
| M-N 레일 배지 상시 켜짐 (D-7) | 1 | `SanctumRailBadge_MirrorsTheTabDots...` † |

† 스윕이 무방비를 찾은 뒤 이번 사이클에 추가한 테스트. 첫 스윕은 **11/14**였다.

### M-A가 가장 중요하다

구 스위트에서 M-A는 **0개**를 빨갛게 만들었다. 배포된 결함을 그대로 복원해도
371개 테스트가 전부 초록이었다는 뜻이다. 지금은 4개가 잡는다.

`LobbyContainmentTests`의 변이 표가 더 날카로운 것을 기록한다: M-A는
390×844와 412×915에서 **초록**이다. 즉 이 스위트가 이미 갖고 있던 폰 뷰포트만
샘플한 담김 테스트는 **같은 결함을 두 번째로 출하했을 것이다.** 잡는 것은
랜드스케이프 행이고, 배포 프레임이 랜드스케이프다.

### 스윕이 찾은 것은 구멍 3개가 아니라 비율이다

무방비였던 결정 3개 — D-3(터치 기하), D-7(배지), D-8(레일 3개 동결) — 는
**전부 만장일치 결정**이다. 논쟁이 있었던 결정 중 무방비는 **0개**다:

| 결정 | 성격 | 최초 방어 |
|---|---|---|
| D-2 티어 삭제 | 디렉터 판정, 대안 기각 기록 | 있음 (M-K) |
| D-4 아이콘 크기 | QA가 스펙 기각, 재계산 | 있음 (M-E) |
| D-10 담김 불변조건 | QA 3건 추가 신고 | 있음 (M-A/M-B) |
| **D-3 터치 기하** | **만장일치, 네 역할 독립 동의** | **없음** |
| **D-7 배지** | **중재 결과, 양측 합의** | **없음** |
| **D-8 레일 3개 동결** | **디자이너 제약, 이견 없음** | **없음** |

**논쟁은 테스트를 낳고 합의는 낳지 않는다.** 반대가 있으면 누군가 근거를
적어야 하고 그 근거가 단언이 된다. 모두가 동의하면 적을 것이 없어 보인다.

D-3이 가장 위험했다. `ApplySortieTouchLayout(true)`를 `(effectiveWidth < 900f)`로
되돌려도 **805/805 초록**이었다:

```
390x844 (모든 터치 테스트가 여기서 빌드)  E=798.7  <900 참   92x92  44.9px PASS
1280x853 배포                            E=1176   <900 거짓  84x28  30.5px FAIL
501x334 레터박스                          E=1176   <900 거짓  84x28  11.9px FAIL
```

모든 터치 플로어 테스트가 **두 아암이 모두 멀쩡해 보이는 유일한 뷰포트**에서
빌드했다. D-11이 명명한 세 번째 실패 모드가, **그 실패 모드를 문서화한 파일
안에** 살아 있었다.

닫을 때 QA가 내 근거를 정정했다. 나는 "배포 프레임에서는 두 아암 모두 44px를
넘으므로 px 단언이 무의미하다"고 했는데 **틀렸다** — 84×28은 30.5px로 지금도
미달이다. 진짜 이유는 더 약하게 들리고 더 나쁘다: px 단언은 배포 프레임에서
`44/1.0884 = 40.4u` 이상만 요구하므로, 데스크톱 아암이 84×44로 돌아오면
47.9px로 통과하면서 D-3의 92×92는 사라지고 레터박스 밴드는 18.7px로 그린다.
**단위가 계약이고 px는 그 파생이다.**

---

## 사이클 중 발견 5건 — 전부 해소 또는 이월 등록

| # | 발견 | 잡은 주체 | 심각도 | 상태 |
|---|---|---|---|---|
| 1 | **회의 내부 모순** — D-3과 D-4가 같은 92u에 다른 실척 적용 | 디렉터 (착수 전 재검증) | **높음** | D-11로 해소 |
| 2 | 부채 표가 2사이클간 도달 불가 컨트롤을 감사됨으로 계상 | SweepUpdateTests | **높음** | 독스트링 기록 |
| 3 | `FindButton("지도")`가 레일 아이콘을 반환 | ContainmentTests | 중간 | 이름 조회로 수정 |
| 4 | 아코디언 정당화가 검증된 적 없음 (542u vs 422u 뷰포트) | SweepUpdateTests | 중간 | 주장 반전, cycle-9 이월 |
| 5 | **만장일치 결정 3건 무방비** (D-3·D-7·D-8) | 디렉터 (변이 스윕) | **높음** | 테스트 4종 추가, 14/14 |

### 1번이 이 사이클의 방법론적 발견이다

D-3은 강하 92u를 **1280 CSS(100.1px)**로 재고 유지했다. D-4는 레일 92u를
**501 CSS(39.2px)**로 재고 기각했다. 같은 회의, 같은 버튼 계열, 정반대 판정.

게다가 D-4가 "501이 진짜 최악"이라 적은 것은 **틀렸다**. 템플릿은 두 밴드를
만들고 fill 밴드 최악은 0.3598(280×653)이다. 103.3u는 D-4가 주장한 "모든 구성
PASS"를 달성하지 못한다 — 320·280에서 미달이다.

근본 원인은 아이콘 크기가 아니라 **지원 하한이 문서 어디에도 없었다**는 것이다.
하한이 없으면 모든 결정이 자기에게 편한 실척을 고를 수 있고, 두 결정이 정확히
그렇게 했다. D-11이 **375×667(iPhone SE2)**로 명명했다.

### 2번은 부채 표 자체가 실패 모드였다

구 배치는 SANCTUM을 y−708에 주차했을 뿐 **비활성화하지 않았다**. 감사는
`activeInHierarchy`로 거른다. 그래서 성소 탭 4개와 성장 "+" 3개가 **아무도
누를 수 없는 동안 감사됨으로 보고**됐다.

11개 항목은 바뀌지 않았다. **그 항목들의 참이 바뀌었다.** cycle-8을 diff하는
독자는 동일한 표를 보고 아무 일도 없었다고 결론지을 것이므로 독스트링에 적었다.

---

## 이월

| # | 항목 | 측정 | 만료 |
|---|---|---|---|
| 1 | 강하 92u, 지원 플로어 375에서 40.3px | −3.7px | cycle-9 |
| 2 | 전체 지도/정비 96u, 375에서 42.1px | −1.9px | cycle-9 |
| 3 | 장비·각인 2중 구현 | 2곳 | cycle-9 |
| 4 | G5 parity 밴드 재측정 | 미측정 | cycle-9 |
| 5 | 아코디언 정당화 미검증 (542u vs 422u) | 77.9% 가시 | cycle-9 |
| 6 | 부채 표 기준 0.488이 실제 최악 아님 | 재기준 시 2행 추가 | cycle-9 |
| 7 | G8 인상 점수 — **5사이클 연속 미측정** | — | 승격 필요 |
| 8 | 레일 아이콘 320 CSS 38.6px (범위 밖) | −5.4px | 무기한 |

**이번 사이클에 남은 항목은 없다.** 초안이 8번으로 올렸던 §4c 육안 확인은
WebGL 빌드 + 헤드리스 크로뮴으로 이행했다 (`qa/lobby-rail-smoke/` 4장,
콘솔 에러 0). 그것이 "미측정"으로 남겨뒀던 워든 가시성 질문에 답했다.

이월 8건은 전부 **다음 사이클 항목**이며 이 게이트를 막지 않는다.
