# Gate Measurements — cycle 2 (run-id 20260805-dungeon-gimmicks)

측정 = 값 + 방법 + 증거 경로. [INFERENCE] 값은 게이트 인용 금지(벤치마크 규칙).

## #g2 — 규칙·밸런스

- 단일 히트 상한: wall tick 8 ≤ 30%×maxHP(최소 100) — 정적 감사
  [OBSERVED, CampaignSpec.WallTickDamage=8]. **지속 노출 결정 기록**: 벽 대역
  잔류 시 이론상 ~12틱(96) 노출 가능 — D3/D4 "DoT는 서 있으면 치명" 관례 채택,
  이탈 속도 여유 218 vs 80 (2.7×). 상한 위반 아님(사유 기록됨).
- 해류 직접 피해 0 — 대체 밴드: 푸시 유발 접촉피해 (EditMode 후 측정).
- TTK/클리어 매트릭스: EditMode 게이트 후 채움 (kiter+rusher @2/1/3, 5/5/5).
- 사전 행동 프로브 [OBSERVED, dotnet 스탠드얼론 — golden-digests-cycle2.md]:
  wallKin 스펙 일치(f9=248, f12=368), wallDmg 8×3 in [10.5,18),
  current drift +447px, pylon down 2.48s(콤보 3스윙 263≥240).

## #g5 — 경제 (밴드: pm/reward-bands.md)

- 첫클리어 보너스 +6/+8/+10 — kiter 평균 런 유물 수입 실측 후 25% 계약 검증.
  (참고: 1800틱 kiter relics 1-4/런 — 30초 단면이므로 풀런 실측 필요.)

## #g7 — 코어 루프

- 루프 주기 모델: design/core-loop.md — N1 6.0s(상위 25-40s), N2 30-60s,
  N3 22.5s 고정. 전부 30-180s 대역 내(상위 루프 기준). 이벤트 트레이스는
  EditMode 후.

## #g8 — 참신성

- 빈도표: tide-current 1/11, ember-pylon 1/11, ash-wall 2/11 — 전부 ≤2/≥5
  기준 통과 [design/trend-survey/dungeon-gimmick-trends.md, QA 분모 검증:
  11 타이틀 ≥ 5 충족]. thin-evidence 셀(current ETG(t), pylon PoE(t))은
  솔루션 문서 출처 라벨 확인 완료 — 반증 출현 시 재판정.
- 인상 점수 ≥4/5: 배포 후 구조화 플레이테스트 (미측정).

## #g6 — ops (텔레그래프 예산 포함)

- 동시 텔레그래프 센서스(사전 산술): sluice 0(위상 3.0s 반주기 오프셋),
  ash-march 최대 2([9.0,9.2) 벽×vent 겹침), bastion vent 1 — 전부 ≤3.
  LCM 센서스 테스트(D3)가 기계 고정 예정.
- EditMode 전체 초록 + WebGL 빌드 ≤120MB: Unity 6000.5.6f1 로컬 설치 완료
  (WebGLSupport 확인) — 게이트 실행 대기.

## v1.1 리튠 재게이트 (2026-08-05)

- EditMode: **225/225** [OBSERVED, unity-logs/test-results-220506.xml] —
  v1.1 상수/배치/듀얼 벽/오라 0.40/센서스/골든 전부 포함.
- WebGL 빌드: `result=Succeeded size=57024061 errors=0` [OBSERVED,
  unity-logs/build-220518.log] (warnings 2건은 main 세션 코드 소유 — 기존).
- 골든 재고정: 신규 3행 Unity 재기록 — **정수 필드는 dotnet과 Unity 완전
  일치**(1400/3/9/0/124 · 2500/3/11/2/112 · 3600/4/16/0/142), float만 교체
  (기록된 ~ULP 드리프트 패턴 그대로). 기존 12행 무변경.
- 브라우저 육안 [OBSERVED, qa/retune-*.webp 3장]: sluice 밴드가 스폰을 물고
  회랑 vent 착탄, march 양측 경계선(엠버)+우벽 침식 오버레이, 벽 틱 피해
  비네트. 헤드리스 스크린샷 스톨 1회는 오토파일럿 interval 과부하로 추정
  [INFERENCE] — 실기기 프로파일은 G6 최종(Stage 3) 항목.
- 텔레그래프 센서스(테스트 고정): sluice 최대 2 · bastion 1 · march 2 (≤3).
- 벽 단일 히트 10 ≤ 30%×100 · 회랑 불변식 gap ≥ 600px(테스트 고정,
  실측 599.99994 — float, 계약 의미 내).

## v1.2 재미 패스 재게이트 (2026-08-05)

- EditMode: **231/231** [OBSERVED, unity-logs/test-results-231022.xml].
  빌드: 57.03MB errors 0 [build-231034.log].
- 골든 v1.2 분리: 무버 5행(1·3·4·5·8) 재고정 — **정수 필드는 dotnet/Unity
  일치**(에코스론 4100/4/16/2/142 등), float만 교체. 불변 안전망
  (아레나×2·프롤로그·스테이지 0·2·6·7·클래식 3) 무변경 [OBSERVED].
- 게이트 중 잡은 것: EmberGallery 링 테스트가 정확한 틱 id를 어서션 —
  fmod/floor 경계 1틱이 런타임별로 갈림(dotnet 통과·Unity 실패). 격자
  패턴(36±1틱 간격+랩 순서) 어서션으로 재작성 → 231/231.
- **난이도 실측 신호 (Stage 2 협상 입력)**: ash-march 골든이 hp 8로 종료
  (v1.1은 142) — 5/5/5 풀장비 kiter가 30초를 간신히 생존. 리튠 체감 달성의
  증거인 동시에 피날레 구간 과열 후보. 사람 플레이테스트에서 사망률 확인 후
  wall tick 10→9 또는 vent 위상 완화를 협상 안건으로.
- 골든 취약성 주지: ash-march 행은 사망 1틱 마진 — reason 필드가 바뀌면
  회귀가 아니라 이 민감도의 산물일 수 있음(테스트 주석에 재고정 프로토콜).

## v1.3 메타 패스 재게이트 (2026-08-06)

- EditMode: **236/236** [OBSERVED, unity-logs/test-results-011502.xml].
  빌드: 57.2MB errors 0 [build-011546.log]. 골든 이동 0 (서약은 옵트인 —
  기본 경로 불변 증명).
- 브라우저 육안 [OBSERVED, qa/v13-*.webp 4장]:
  - 성장 탭 실수치 검산 일치: 공격력 76.7(+2.1) = 58×(1+0.03×4)×(1+0.06×3),
    체력 156 = 100+32+24, 이동 227 = 218×1.04. HackConfig 프로퍼티 직독 —
    뷰 리터럴 0 (테스트 가드 고정).
  - 장비 탭: 티어명(선고날 T3 등) + "유물 11 → 공격 +3.9" 델타 표기.
  - 서약 런: HUD "Cinder Span — 서약", 서약 배치(추가 vent) 적용.
  - **서약 유물 ×2 실측**: 클리어 시 relics 41→57 (+16) = 인런 8×2 정확.
    첫클리어 보너스 비지급(기클리어) — negotiation entry 5 계약 그대로.
- 폰트: U+2713 소스 폰트 부재 발견 → "서약 •"(U+2022, 시드 문자)로 교체,
  재생성 coverage FULL (v1.2 잔여 5자 + v1.3 8자 포함).

## v1.4 이월 결함 패스 재게이트 (2026-08-06)

v1.3 문서가 남긴 이월 항목을 코드로 닫은 패스. 신규 기능 0 — 결함 수정과
**주장의 기계화**(문서에만 있던 계약을 테스트로 고정)만.

- EditMode: **240/240** [OBSERVED, unity-logs/test-results-093217.xml]
  (v1.3 236 + 신규 4). 골든 이동 0 — 15행 전부 무변경.
- WebGL 빌드: `result=Succeeded size=57006939 errors=0 **warnings=0**`
  [OBSERVED, unity-logs/build-092640.log]. v1.3의 경고 2건이 0으로 —
  cycle-2 회고가 "main 세션 코드 소유"로 이월했던 항목 종결.
- 최종 재확인 [OBSERVED, test-results-094336.xml **240/240** ·
  build-094351.log `errors=0 warnings=0 size=57006779`].
- 라이브 스모크 (로컬 `python3 -m http.server 8917`, 헤드리스 Chromium
  1280×720, 약 10분): 부팅 → 로비 렌더(성장 탭 실수치 `공격력 58.0 (+1.7)`
  · `최대 체력 100 (+8)` · 요약줄 정상) → 프롤로그 클리어 → 세이브 반영
  (`cinder-span 강하 가능`) → 던전 진입 → **스테이지 시작 말풍선 렌더**
  (감시자, 앰비언트) → 웨이브 5/5 진행. **페이지 에러 0건.**
  이 경로가 이번 패스에서 건드린 View 3파일(LobbyView 부팅 경로 ·
  HudView 필드 · SpeechBubbleView Show)을 전부 통과한다.

- **보스 엠버 틴트 육안 확인 — 닫힘 (2차 스모크)**. 1차 스모크의 미도달
  항목이었다. 막힌 원인은 게임이 아니라 하니스였다: 블라인드 입력 봇은
  8-9웨이브 던전을 못 깬다. 해소 레시피(다음 스모크에 재사용):
  ① Unity WebGL은 **localStorage**에 저장한다(IndexedDB 아님) — 부팅 전
  `abyssal-lantern:unity:campaign` 키에 세이브 JSON을 심으면 진행도·장비를
  임의 지정 가능. ② `?mode=campaign&stage=<id>` 딥링크로 즉시 진입.
  ③ **동료를 활성화(active + G 홀드)** — 자율 DPS가 없으면 봇은 웨이브 1에서
  처치 0으로 죽는다(실측 2회).
  결과 [OBSERVED]: cinder-span 보스 도달, `CINDER WARDEN` 말풍선 렌더.
  화자 라벨 픽셀 측정 — 밝은 250px 평균 **hue 12.5°**,
  EmberColor(#F3592C) 13.6° 대비 **Δ1.1°**, WatcherColor(#DDC869) 49.1°
  대비 Δ36.6°. **리팩터한 Boss 분기가 라이브로 엠버를 칠한다**는 측정 증거.
- **잔여(축소됨)**: cycle-2 보스 말풍선 자체의 스크린샷. Sluice Keeper는
  라이브 도달 확인(HUD 보스바 `Sluice Keeper PHASE I/III`, 처치 72)했으나
  봇이 보스전에서 사망해 말풍선 프레임을 못 잡았다. 남은 위험은 0에 수렴:
  같은 함수·같은 enum·같은 상수이고, SluiceKeeper→Boss 매핑은 9스테이지
  순회 테스트가 고정한다. 사람 플레이 시 확인 항목으로만 남긴다.
- **부수 실측 — ash-march 치사성 (v1.2가 남긴 '피날레 과열' 안건의 라이브
  증거)**: 풀장비(10/10/10, 5/5/5, 최대체력 220)로 딥링크 진입 후 **무입력
  20초에 체력 220 → 16**. 골든의 "hp 8로 생존" 신호와 같은 방향이다.
  플레이테스트 전이라도 협상 안건의 근거로 쓸 수 있는 수치.

### 닫은 결함

1. **말풍선 화자 색 (회고 이월 #2)** — cycle-2 보스 3종(SLUICE KEEPER /
   BASTION SENTINEL / ASH MAGISTRATE)이 앰비언트(감시자) 색으로 렌더되던
   결함. 원인은 이름 **prefix 매칭**: 화자 정체성은 StoryCatalog가 갖는데
   색 규칙은 3파일 떨어진 SpeechBubbleView의 prefix 목록에 있어, 보스를
   추가해도 아무것도 실패하지 않았다. `StoryCatalog.VoiceOf`(상수와 같은
   자리의 정확 switch) + `SpeakerVoice`로 이동 — prefix 매칭 제거.
   가드: `StorySpeakers_ResolveTheirIntendedBubblePalette` — 라이브
   StageCatalog 9스테이지 × 4비트를 순회해 **명명 화자는 절대 앰비언트로
   칠해지지 않음**을 고정(+ "CINDER IMPOSTOR"가 앰비언트로 떨어지는지로
   prefix 회귀 차단). 실측 중 발견: abyss-chancel completion 화자는
   DUSK WARDEN이 아니라 VEIL TACTICIAN(보스가 마지막 말) — 데이터가 아니라
   테스트의 가정이 틀렸던 것이라 테스트를 고쳤다.
2. **CS0414 죽은 필드** — `_stageClearShownScore/_stageClearShownRelics`는
   기록만 되고 읽히지 않는 카운트업 잔재(스펙에 카운트업 없음) → 삭제.
3. **CS0618 폐기 API** — `FindFirstObjectByType` → `FindAnyObjectByType`
   (LobbyView 1 + 테스트 4). 전부 존재 확인용이라 순서 무관.

### 기계화한 주장 (이전엔 문서·스크린샷만)

4. **서약 경제 (negotiation entry 5)** — GameDirector 주석은
   "internal so the EditMode economy test pins it"이라 했으나 **그런 테스트는
   없었다**(테스트 레인도 "EditMode 밖"으로 분류). 실제 로비 토글을 눌러
   출정하는 라우트 테스트 신설:
   `ArmedPactSortie_DoublesOnlyTheInRunRelicPayout`.
   측정 [OBSERVED, test-results-105649.xml 출력 블록 — origin/main 머지 후
   재측정]: 서약이 **자기 런 유물 3 → 6 지급 = 2.00×**. 함께 고정: 서약
   지급은 인런 유물에만 배수(보너스 라인 비배수), 반복 클리어 포인트 +2
   (첫클리어 +3 아님), 진행도 불변.
   **정정(머지에서 배움)**: 최초 판(v1.4)은 "일반 런 3 → 서약 런 6, 2.00×"로
   *런 간* 총액 비율을 게이트로 걸었다. 머지 후 이 게이트가 깨졌는데
   (일반 1 · 서약 6 = 6.0×), **배수는 여전히 정확히 2**였다 — 움직인 건
   두 런의 인런 유물 수급(1 vs 3)이다. 유물 드랍은 처치한 적 id에 걸리므로
   런마다 다르고, **n=1로 "평균"을 주장할 수 없다.** 게이트는 뷰가 실제로
   통제하는 것(자기 런 대비 배수 ≤2.2×)만 걸고, 런 간 총액은 게이트가
   아니라 **관측치로 출력**하도록 고쳤다. entry 5의 "평균" 조항은 표본
   연구가 필요한 **QA 미결 항목**으로 되돌린다 — n=1로 충족 처리했던 것이
   과장이었다.
5. **로비 rect 감사** — v1.3이 "픽스처가 없어 후속"으로 미룬 항목.
   `LobbyLayoutTests` 신설(WorldSpace 캔버스 시임 — HudLayoutTests와 동일).
   서약↔강하 비중첩은 **실측 통과**.

### 새로 드러난 것 — 사람 판단 필요 (설계 협상)

- **로비 전체가 44 CSS px 터치 하한 미달** [OBSERVED, 위 XML 출력 블록,
  390×844 portrait · 0.488 CSS px/u]: 강하/서약 41.0×13.7 · 탭 58.6×21.5 ·
  스탯 + 25.4×21.5 · 재훈련 54.7×21.5. **전 항목이 세로축 미달.**
  SIM_SPEC_HACKSLASH §9 "버튼 최소 44px" 위반이며 **v1.3 이전부터 존재**
  (서약 토글은 강하 행 문법을 복제했을 뿐). v1.3 테스트 레인 문서의
  "≥44 css px at phone scale" 주장은 산술 오류 — 해당 문서에 정정 기록.
  68 u 카드 피치·9카드 스크롤·탭 스트립이 함께 움직이는 변경이라 조용히
  재배치하지 않고 **동결 래칫**으로 고정: 미달 집합을 정확히 핀했으므로
  신규 위반은 테스트가 잡고, 실제 수정도 테스트를 깨워 협상 기록을 요구한다.
  → designer+pm 안건.

### 이월 유지 (이 패스 범위 밖)

- G8 인상 점수·G7 repeat-rate: 배포 후 구조화 플레이테스트 필요(불변).
- ash-march 피날레 과열(골든 hp 8 마진): 사람 플레이테스트 후 판정(불변).
- gh-pages 배포·origin push: 자격증명 권한 문제(불변, 사람 판단).

## origin/main 머지 재게이트 (2026-08-06, dungen ← origin/main 3d8727b)

20커밋 유입(타 세션: 입력 심도 3·5, hold charge 버그 수정, additive glow,
오디오 보이스풀, MCP 툴링 수렴, UI 플레이트). **소스 충돌 0**, 머지 커밋
`523545c`. 게이트: **EditMode 266/266** [OBSERVED, test-results-105649.xml]
· 빌드 `errors=0 warnings=0 size=57469558` [build-105722.log].

### 머지가 잡은 것 (이게 골든의 존재 이유)

골든 테스트는 **이 브랜치에만 있다**. origin/main 세션은 자기 게이트
225/225 초록으로 정상 진행했지만, 그 심 변경이 이쪽 다이제스트 14행을
움직인다는 사실을 볼 방법이 없었다. 머지 시점에 처음 드러났다.

- **원인**: `5fe0059 fix(sim): charge was unreachable`(홀드 시 콤보 완주 후
  차지 시작) + `3d599f0 feat: input depth 3 and 5`. 봇들이 `AttackQueued`를
  매틱 넣으므로 전투 흐름이 통째로 바뀐다. **의도된 변경**(스펙
  `design/input-depth-spec.md`, SIM_SPEC_HACKSLASH §3 갱신)이라 되돌릴
  대상이 아니다.
- **제 변경이 원인이 아님을 증명**: v1.4 커밋 3개가 건드린 파일에
  `Assets/Scripts/Sim/` **0개** (뷰·테스트·문서만).
- **처리**: 골든은 계약이 아니라 녹화본이므로 Unity 런타임에서 재기록
  (`GoldenDigestRecorder.Record`) → **14행 재고정**(classic-abyss-chancel만
  불변). 레코더 출력과 테스트 실패 메시지의 actual 행이 일치함을 교차 확인 —
  레코더 문서가 요구하는 "테스트가 권위" 조건 충족.
- **주목할 이동**: **ash-march hp 8 → 52.** v1.2가 "풀장비 봇이 간신히
  생존"으로 플래그한 피날레 과열이 저쪽 charge 변경으로 **일부 완화됐다**.
  과열 안건의 입력값이 바뀌었으니 협상 전에 재측정할 것.
- 부작용 관찰: 재고정 후 여러 행의 score/wave/kills/relics가 같은 값
  (3700|4|15|2)으로 수렴했다 — 체력·좌표는 여전히 스테이지별로 갈리지만,
  정수 4필드의 변별력은 예전보다 낮다. 골든의 민감도가 떨어진 것이니
  다음 사이클에서 봇 스크립트 다양화를 검토할 것.

### 머지가 잡은 것 2 — mutation 프로브 실명

`NewStages_MutatedPlacement_ChangesDigest`의 bastion 레시피("첫 기둥 x+50")가
**장님이 됐다**(변이해도 결과 불변 = 아무것도 증명 못 함). v1.1에서 같은
일이 있었고 그때도 재프로브로 해결했다. 6해저드 × 6변이 스윕 [OBSERVED]:
(640,650) 기둥은 스크립트 진동 경로 밖, **(900,560) 기둥은 경로 위**
(x+50 → score 2450→3050, kills 10→12). 좌표로 명시 지정해 테이블 재정렬
시 다시 장님 기둥으로 흘러가지 않게 했다.

### 워킹트리 취급 (계약 §5)

다른 세션 소유 5파일(.vscode/settings.json · HongT.slnx · Packages/
manifest.json · packages-lock.json · Data/Plugins/lib_burst_generated.wasm)은
**머지 전 바이트를 md5로 백업 → stash → 머지 → 바이트 그대로 복원**했다.
packages-lock 은 stash pop 에서 충돌했으나(워킹트리는 coplaydev MCP v10,
origin/main 은 ivanmurzak) **병합하지 않고 워킹트리 바이트를 복원** — 소유
세션 상태가 진실이고 이 세션이 판단할 사안이 아니다. 5파일 전부 md5 일치
확인. 복귀 지점: 태그 `pre-merge-origin-main-20260806`.

## v1.5 각인 패스 재게이트 (2026-08-06, AMENDMENT #6)

요청: "강화쪽도 기믹 관련과 전투 및 던전 추가 요소". v1.3을 표시 개선으로만
좁게 읽었던 것을 정정하는 패스다.

- EditMode: **290/290** [OBSERVED, unity-logs/test-results-121833.xml]
  (머지 후 266 + 각인 14 + 나머지 신규). **골든 7건 무이동** — 미장착 경로
  바이트 동일 증명.
- 빌드: `result=Succeeded size=57444053 errors=0 warnings=0`
  [OBSERVED, build-121903.log].

### 조사가 먼저였다

기존 서베이 2건은 **던전 기믹만** 다뤘고 강화-기믹 접점은 아무도 조사하지
않았다(공백 확인 후 신규 조사 실시):
`.survey/meta-upgrade-gimmick-interaction/` (validator PASS, provenance 라벨
포함). 핵심 결론 3개가 그대로 설계 규칙이 됐다:

1. **면역 금지, 저항까지** — 완전 면역은 도전을 삭제한다. 판정선은 "기믹이
   여전히 행동을 바꾸는가".
2. **무작위 발동 금지** — D4 아플릭션 반감 vs HoT 유령벽 칭송의 차이는
   예측 가능성. 우리 무RNG 시간표가 조건 판정을 결정론적으로 만든다.
3. **사이드그레이드 우선** — Hades Mirror 문법. 직상향은 인플레.

조사가 확인한 공백: **환경 기믹 상호작용을 메타 층에 붙인 사례가 표본에서
희소하다.** 설계 실무 문헌은 4분류(Conversion/Redirection/Amplification/
Active mitigation)를 서술하지만 로그라이트는 대부분 인런 아이템으로 처리한다.
우리는 고정 시간표 + 대칭 규칙을 이미 갖고 있어 이 자리가 유리하다.

### 구현 — 각인 5종 × 양면, 슬롯 2

| 각인 | 기믹 | A면 | B면 |
|---|---|---|---|
| 역류인 | 해류 | 내 밀기 ×0.5 | 적 밀기 ×1.5 |
| 판결인 | 방벽주 | 오라 0.40→0.70 | 방벽주 피해 ×2 |
| 집행인 | 벽 | 내 틱 10→6 | 적 틱 10→18 |
| 점화인 | 분출구 | 피격 시 기름 +12 (**피해 불변**) | 적에게 피해 14 |
| 증언인 | 제단 | 채널 1.2→0.8초 | 기름 +18→+30 |

심 훅 5개 전부 기존 단일 지점. 각인은 생성자에서 **1회 해석**되어 필드에
캐시되므로 per-tick 비용은 필드 읽기 하나다.

### 실측 [OBSERVED — 스탠드얼론 dotnet 프로브, 전 10면]

| 면 | 측정 |
|---|---|
| 역류 A | 6초 사이클 드리프트 589 → 322 px (절반, **0 아님**) |
| 역류 B | 적 평균 x 508 → 584 |
| 판결 A | 같은 스크립트에서 처치 8 → 9, hp 99 → 123 |
| 판결 B | 방벽주 hp 제거 482 → 600 |
| 집행 A | 우측 밴드 3초 체력 손실 50 → 30 |
| 집행 B | 적 누적 피해 364 → 452 (1.24×) |
| 점화 A | **피해 동일**(38), 피격 틱 기름 0.39 → 10.55 |
| 점화 B | 무공격 런에서 적 피해 0 → 발생 |
| 증언 A | 첫 축복 tick 145 → 97 |
| 증언 B | 축복 tick 동일, 기름 지급 증가 |

### 게이트 중 잡은 것 (전부 테스트 결함, 구현 아님)

- 점화 A 첫 판: 창 전체의 순증을 봐서 **랜턴 캡이 환불을 세탁**했다. 두 번째
  판은 창 안 최대 점프를 봤는데 그건 **기름 플라스크 픽업(41)**이었다.
  최종: 체력이 실제로 떨어진 틱만 골라 그 틱의 기름 델타를 적산.
- 점화 B: dotnet 프로브는 처치 4, Unity는 0 — **처치는 칼끝**이라 런타임이
  갈린다(기록된 float 벽). 계약은 "분출구가 적을 때릴 수 있는가"이므로
  누적 피해로 바꿔 고정.
- 로비 래칫이 신규 탭을 잡았다 — **설계대로 동작**. 탭 스트립을 4×91u로
  재분할하면서 탭 **폭이 58.6 → 44.4 CSS px로 하한을 넘었고**, 높이 21.5는
  그대로라 부채 표에 남는다(각인 탭도 같은 이유로 합류).

### 폰트

신규 한글 15자 추가 → 재생성 491글리프. 소스 폰트에 U+00B7(·)이 없어
제 문자열은 U+2022(•)로 교체. **LobbyView 커버리지 FULL.** HudView에 남은
· 4건은 타 세션 소유(origin/main GrowthChoice 포함)로 미접촉.

### 미해결

- **각인 가격 미서명** — negotiation entry 6. 효과 수치는 협상 대상이 아니고
  (심 수치 + 테스트 고정), 가격 12유물만 PM 확인 대기.
- **라이브 육안 미실시** — 각인 탭 렌더/구매/장착 흐름은 다음 스모크 항목.

---

## v1.6 훈련장·돌발 게이트 (2026-08-06, AMENDMENT #7)

run-id `20260806-training-and-upgrade`. 측정 = 값 + 방법 + 증거 경로.

### 실행한 게이트 [OBSERVED]

| 게이트 | 값 | 증거 |
|---|---|---|
| EditMode | **319/319** (직전 295 + AMENDMENT #7 커버리지 23, 타 세션 1) | `unity-logs/test-results-151826.xml` |
| 신규 커버리지 | `TrainingSurgeTests` **23건 전부 Passed** · 123 어서션 | 같은 XML |
| 골든 | **7스위트 전 행 무이동** | 같은 XML — `Golden_*` 7건 Passed |
| WebGL 빌드 | **57.5MB · errors 0 · warnings 0 · 47.2s** | `unity-logs/build-151907.log` |
| 컴파일 | error CS **0** | `unity-logs/import-141947.log` |
| 폰트 | **497 글리프, v1.6 문자열 MISSING 0** | `tools/gen_hud_font.sh` |

### #g7 코어 루프 — 돌발 주기 [OBSERVED, dotnet 프로브]

방법: 최대 메타(스탯 10/10/10, 장비 T5), 해저드 회피 + 추격 파일럿, 240초 상한.

| 스테이지 | 처치 | 웨이브 | 위기 | 기세 | 첫 기세 | 종료 |
|---|---|---|---|---|---|---|
| cinder-span | 14 | 3 | 1 | 1 | **33.3s** | overrun @44s |
| cinder-sluice | 13 | 3 | 1 | 1 | **58.3s** | overrun @68s |
| ember-bastion | 4 | 2 | 1 | 0 | — | overrun @32s |
| ash-march | 8 | 2 | 1 | 0 | — | overrun @29s |

**판정 및 한계**: 첫 기세 33.3~58.3s는 루프 대역 30-180s **안**이다. 단
4스테이지 중 2곳은 프로브 파일럿이 12킬 전에 죽어 기세를 못 봤다 — 이는
**봇 실력 한계이지 설계 실패라고 단정할 수 없다**. 사람 플레이 실측 전까지
"생존 시 대역 내"까지만 주장한다. [사람 판단 항목]

### #g3 플레이어 유형 [OBSERVED]

| 유형 | 처치 | 기세 | 첫 기세 | 종료 |
|---|---|---|---|---|
| rusher | 14 | 1 | 33.3s | @44s |
| kiter | 28 | 2 | 25.0s | @65s |

둘 다 돌발 문에 도달 — 유형 편향 없음. kiter가 오래 살아 더 많이 본다.

### #g2 밸런스 — 위기 조항 회피량 [OBSERVED]

방법: 동일 스테이지·동일 스크립트 락스텝, 각인만 차이. **양쪽 생존 구간만
유효**(둘 다 죽으면 델타가 0으로 붕괴 — 30s/45s 노출은 그래서 무의미).

| 노출 | 무각인 HP | 집행인 HP | 회피 | maxHP(220) 대비 | 밴드 ≤30% |
|---|---|---|---|---|---|
| **20.0s** | 7.0 | 61.0 | **54.0** | **24.5%** | **PASS** |

director 중재(negotiation entry 8)의 사전 산술 25%와 실측 24.5% 일치.
역류인·증언인은 직접 피해 회피가 아니므로 밴드 무관(회피 0 실측).

### #g5 경제 — 훈련장은 런당 0을 지급한다 [OBSERVED]

15런(5시련 × 3등급) 전수: **유물 0 · 처치 0.** 훈련장이 적을 스폰하지 않아
드롭 경로 자체가 없다 — PM이 지적한 "훈련 심이 이미 유물을 굴린다" 문제의
구조적 해결. 숙달 보상 +2는 뷰 레인 1회성(전 시련 판결 등급 게이트).

### #g8 등급 사다리 — 시계만 움직이고 예고는 고정 [OBSERVED]

| 등급 | 배율 | 60초 펄스 | 실효 분출 주기 | 예고 |
|---|---|---|---|---|
| 견습 | 1.00× | 100 | 2.40s | **0.80s** |
| 숙련 | 0.85× | 85 | 2.82s | **0.80s** |
| 판결 | 0.70× | 70 | 3.43s | **0.80s** |

펄스 수가 배율에 정확히 비례. 예고 상수는 불변 — 조사 표본 어느 타이틀도
예고 단축을 난이도 레버로 쓰지 않았다는 결론의 기계적 이행.

### 게이트가 잡은 결함 4건 (전부 수정 후 재측정)

이번 사이클의 실질 산출이다. **네 건 다 사람이 아니라 게이트가 찾았다.**

| # | 결함 | 잡은 주체 | 처리 |
|---|---|---|---|
| 1 | 컴파일 2종(`using` 누락, `MakeText` 오타) — 플레이 불가 | Editor.log | 수정, import EXIT=0 |
| 2 | **`_trainingRate` readonly 초기화 누락 → 값 0 → 전 기믹 시계 정지** | EditMode 8건 + 골든 1행 | `= 1f` 초기자 |
| 3 | **로비 신규 최소 터치 타겟 28.3 CSS px** (기존 최악 41.0) | LobbyLayoutTests 래칫 | 테이블 갱신이 아니라 **설계 변경** — 등급 선택을 공유 카드 1장으로, 시련 버튼은 강하와 동일 41.0 |
| 4 | **`_kills % 12` 정확일치 → 멀티킬 틱이 임계 건너뜀** (14킬에 기세 0회) | dotnet 프로브 | 교차 판정으로 교체, 재측정 1회 발동 |

결함 4는 특히 뼈아프다: **같은 종류의 임계 건너뜀을 위기에는 가드로 막아놓고
기세에는 빠뜨렸다**(QA 플랜 T2.4가 경고한 바로 그 실패). 두 문이 같은 위험을
공유한다는 것을 스펙에 적어놓고도 한쪽만 구현했다.

### 라이브 브라우저 검증 [OBSERVED, 2026-08-06 headless Chromium 1280×800]

로컬 서빙(`build-webgl`, http 8199) + Unity WebGL 실기동. **테스트가 통과한
뒤에 브라우저가 결함 7건을 더 잡았다** — 전부 뷰 레인, 전부 수정 후 재확인.

| # | 결함 | 증상 | 처리 |
|---|---|---|---|
| 5 | **훈련이 아레나 HUD로 렌더** | 기믹 4기가 **아예 안 그려지고** 스킬행·레벨 없음 | `_dungeonPresentation = _isDungeon \|\| _isTraining` — 심 분기는 `_isDungeon` 유지, 프레젠테이션만 공유 |
| 6 | **서지 배너가 화면에 없음** | 코드는 매 프레임 돌지만 그려지지 않음 | 부모가 `transform`(MonoBehaviour) — 캔버스 밖. `_safeRoot`로 재부모 |
| 7 | 숙달 라벨이 등급 버튼에 가림 | "5시련 판결 완주 →"에서 잘림 | 3버튼 옆 여유 **76u** 실측 → 라벨을 프롤로그 카드(여유 232u)로 이동 |
| 8 | 제목 `— 웨이브 0/0` | 시련엔 웨이브 없음 | `_campaignTotalWaves <= 0`이면 접미사 생략 |
| 9 | 우상단 `웨이브 1 / 유물 0 / 적 0` | 구조적으로 고정된 값이 고장처럼 읽힘 | 시련에서 3줄 숨김 (`SetTrialMode`) |
| 10 | **해류 시련 두 대역이 같은 방향** | 시련의 학습 목표(순류/역류)가 전달 안 됨 | `CurrentPushSign`이 `CampaignStages`만 스캔 → `TrainingTrials`도 스캔 |
| 11 | 해류 통로 20px, 플레이어 안 보임 | 두 오버레이 사이에 끼임 | 배치를 shipped cinder-sluice와 동일(470/740, 통로 50px)로 |

추가로 문구 결함 3건: 시련 종료가 "구역 정화 · 점수 0 • 유물 0"(실패처럼 읽힘)
→ **"시련 완료 · 피격 N회"**, 시련 사망이 "군단에 함락됐다 · 웨이브 1 도달"
(군단도 웨이브도 없음) → **"시련 중단 · 기믹에 쓰러졌다"**, 방벽 시련 레슨이
"오라 안팎의 피해 차이"(오라는 **적** 보호 → 적 없는 시련에서 성립 불가)
→ **"3기 파괴 순서와 이동선"**(스펙 표의 성공 판정과 일치).

**세리머니 제목을 빈 문자열로 만든 수정이 테스트를 깨뜨렸다** —
`PresentationFeedbackTests`가 패널을 자식 텍스트 "구역 정화"로 식별한다.
기본값을 되돌리고 시련일 때만 교체하도록 재수정. 게이트가 제 수정을 잡은 사례.

검증한 것 [OBSERVED, 스크린샷]:
- 시련 5종 전부 기믹 렌더 (분출구 4기 위상차, 해류 대향 `>` / `<`,
  방벽주 3기 오라, 벽 양측, 제단 2기+분출구)
- 등급: 견습/숙련/판결 선택 상태 표시, 제목에 반영
- 60초 시계 종료 → "시련 완료 · 피격 0회"
- 세이브: `trialTiers` 0 → 1(견습) → 3(판결 갱신), 유물 40 **불변**
- 로비 기록: "최고 판결" / "최고 견습" / "미도전" 3상태 정상
- 던전 **위기 배너 "위기 0.1"** 실물 확인, 사망 시 사라짐

**확인 못 한 것**: 기세(12킬) 배너의 화면 표시. 헤드리스 키보드 조작으로
12킬 전에 계속 사망했다. 트리거는 프로브+회귀 테스트가, 라벨 렌더는 위기가
같은 Text·같은 코드 경로로 각각 증명하지만 **기세 문자열 자체는 미관측**이다.

### 미해결 / 사람 판단

- **기세 배너 육안** — 위 사유로 미관측. 사람 플레이 1세션에 포함.
- **기세 도달률** — 봇이 2/4 스테이지에서 12킬을 못 넘겼다. 12가 적정 간격인지
  사람 플레이 확인 필요. 낮추면 잦아지고 올리면 후반 전용이 된다.
- **각인 가격 12유물 미서명** — entry 6. 이번 사이클에 **좁혔으나 못 닫았다**:
  상수 모델을 만개 앵커 r=17.67로 재계산해 PM의 독립값 **c=17**을 재현했고
  (c=12는 8.83런, 하한 10 위반), c=12가 통과하려면 `r ≤ 15.60 유물/런`임을
  명시했다. 부트스트랩 곡선 임계 0.714는 **클리어 런** 수입이 필요한데
  프로브 파일럿이 6스테이지 × 6랭크에서 **0/6 클리어**라 앵커를 못 만들었다.
  부분런 수입 3.5~4.5는 실패한 런의 값이라 인용 금지 — 인용하면 모든 c가
  통과로 나와 판정 자체가 무의미해진다. negotiation entry 6-update 참조.

---

# v1.7 — 진행 네비게이션 (run-id 20260807-progression-navigation, AMENDMENT #8)

측정 2026-08-07. EditMode 최종 `engineering/unity-logs/test-results-125635.xml`
(**320/320, 실패 0**). WebGL 빌드 `build-125231.log` (errors 0).
브라우저 스모크 증거 `qa/nav-smoke/*.png`, 로컬 서빙 `python3 -m http.server 8099`.

**성격**: 이 개정은 순수 뷰다. 심 수치 0, 저장 필드 0. 따라서 골든 무이동이
PASS 조건이 아니라 **전제**다.

## g1 — 세계관 일관성

- 신규 플레이어 가시 문자열 전수와 worldview 추적성:
  `제1부 기록`/`제2부 증언`/`제3부 집행` = worldview §공간 계보 부(部) 이름 그대로.
  `다음 재판`·`선행:`·`점화 훈련 필요`·`휴면`·`대기`·`다음 재판 대상` = 법정 어휘.
  **신규 고유명사 0.** [OBSERVED — 소스 grep + 스모크 육안]
- 폰트 커버리지: **500 글리프, coverage FULL** (`bash tools/gen_hud_font.sh`).
- **결함 4건 발견·수정** [OBSERVED]:

| 문자 | 위치 | 성격 | 해소 |
|---|---|---|---|
| `간` U+AC04 | ProgressionGuide.cs 주석(신규) | 이번 사이클 유입 | 재생성 |
| `·` U+00B7 | HudView 가시 문자열 4건 (**기존, 라이브**) | 소스 폰트에 **없음** | `•` 치환 |
| `−` U+2212 | HudView:889 (**기존, 라이브**) | 생성기 정규식이 문자열 통째 누락 | ASCII `-` 치환 |
| `≥` U+2265 | 소스 스캔 | charset엔 있고 폰트엔 없음 | 재생성 |

`·`와 `−`는 **재생성으로 닫히지 않는다** — NanumBarunGothic cmap에 부재 확인.
QA 레인의 첫 해소 경로표가 `·`를 "재생성 가능"으로 적었고 실측이 뒤집었다.

**판정: PASS**

## g2 / g3 / g5 — 밸런스·유형·경제

밸런스 수치 미접촉. G2/G3는 이월(변경 없음). G5는 아래.

- `badge_misdirect_relics ≤ 2`: **최악 0** (전수 283,392 상태 = Relics 0..40 ×
  티어 216조합 × 각인 마스크 32; 정의역 251,655). 현행 "살 수 있음" 의미론의
  실측 위반값은 **10**이었다 (T0/T0/T0 + 유물 12). [OBSERVED — T-A4]
- 유물 탭 동시 점등: **0건 / 283,392**. 두 분기 모두 실행됨(장비 244,463 ·
  각인 7,192)이라 공허하지 않다. [OBSERVED]
- 스모크 실측: 유물 12 보유 시 장비 배지 ON / 각인 배지 OFF
  (`03-tab-badges.png`) — 각인 12를 살 수 있는데도 더 싼 장비 7을 가리킨다.
- `paid_free_winrate_delta`: **정의 불가** — IAP/billing 패턴 매칭 0건이라
  코호트 분할이 성립하지 않는다. 웨이버 유지, 대체 지표는 정보 공정성
  (전 플레이어 동일 정보) = delta 0.

**판정: PASS** (G2/G3 이월, G5 밴드 충족)

## g4 — 이펙트·애니메이션 몰입

로비 전용 사이클이라 전투 연출 미측정. 이월.
단 **정적 가독성**은 측정했다:

- 사유 라벨 대비 **8.71:1** (InkDim on 숯불), AA 4.5 통과. [OBSERVED]
- `ignoreParentGroups` 제거 시 **2.48:1**로 미달 — 음성 검정으로 이 배치가
  하중을 받는다는 것을 증명. [OBSERVED — T-A6]
- 잠금 회색은 원색 4.38로 **애초에 미달**이라 사유 텍스트에 쓸 수 없었다.

**판정: 이월** (전투 연출 미측정)

## g6 — 운영

- EditMode **320/320**, 실패 0. 기존 303 → 320 (신규 17, 감소 0).
- 뮤테이션 검정 **5/5 검출**, 위양성 0 (STAMP 125008/125610). 공허한 테스트
  없음의 기계적 증거. [OBSERVED]
- WebGL 빌드 errors 0, `build-webgl` **38 MB** (한도 120 MB).
- 브라우저 콘솔 에러 **0건** (첫 로드 + 시드 리로드).
- 터치 래칫: 4폴드 상태 합집합 **30 컨트롤**, 최협 25.4 / 최단 13.7 —
  **둘 다 불변**. 신규 위반 등급 0. [OBSERVED]

**판정: PASS**

## g7 — 코어 루프

네비게이션은 루프를 만들지 않고 **루프로 되돌리는 경로**를 만든다.

- 아코디언 기하 실측: 액트 펼침 **416.0u**, 훈련장 펼침 **626.0u**,
  뷰포트 **434.0u**. spec §2.2와 정확히 일치. [OBSERVED — T-A7]
- 가시율 **41.0% → 100%** (액트) / **69.3%** (훈련장).
- 지목 슬롯: 1024 상태 전수에서 슬롯 수 ∈ {0,1}. `None`은 전 클리어 +
  숙달 완료에서만. [OBSERVED — T-A1]
- `mastery_surface_rows_visible ≥ 1`: **착지 전 0 → 착지 후 3**
  (`06-training-fold.png`; 등급선택 [204,272] · 시련1 [274,342] ·
  시련2 [344,412] 완전 가시, 시련3 [414,482]은 434 초과로 부분).
  **밴드 metric은 등급선택을 포함한다** — 등급선택은 시련 진입의 전제이고
  `interactable`도 같은 `open = PrologueDone`이 결정한다(`LobbyView.cs:857-858`).
  QA 레인이 문구 모호를 지적해 확정 해석을 test-plan에 고정했다. min 1이라
  이번 판정은 안 갈렸으나 하한이 2 이상이면 해석 차가 판정을 뒤집는다.

**판정: PASS**

## g8 — 참신성

| 후보 | 빈도 | 인상 |
|---|---|---|
| N11 컨텍스트 라벨 | **0/17** | 스모크 확인 (`04-sigil-context.png`) |
| N2 공개형 지목 | 0/6 | 스모크 확인 (`02-act3-autofollow.png`) |
| N5 배지 | 1/6 | 보조 |

- 각인 도달률 전수 실측 [OBSERVED — T-A8]:
  점화인 **9/9** · 증언인 4/9 · 판결인 3/9 · 역류인 2/9 · 집행인 **1/9**.
  PM의 9배 스프레드 주장이 양끝 모두 확인됐고, 중간 3종도 새로 측정돼
  분모가 채워졌다.
- 스모크에서 라벨이 실제로 분화함: 대상 cinder-sluice(해류+분출구)에서
  역류인·점화인 골드 `다음 재판 대상`, 판결인·집행인 InkDim `휴면`.
  **심의 실효 해저드 표와 화면이 일치한다.**

**인상 점수는 여전히 미측정** — cycle-3와 같은 지점이다. 구조 확인은
브라우저로 끝났으나 5점 척도 채점은 사람 세션이 필요하다.

**판정: 빈도 PASS / 인상 미측정 → 조건부**

## 브라우저 스모크 체크리스트 (cycle-3 회고 §4b가 게이트로 승격)

| # | 항목 | 결과 |
|---|---|---|
| 1 | 첫 로드 렌더 + 콘솔 에러 | PASS — 에러 0 |
| 2 | 아코디언 4그룹 헤더 표시 | PASS |
| 3 | 카운트 정확 (3/3·3/3·0/3·0/5) | PASS |
| 4 | 자동 추종 — A6 아키타입(6클리어)에서 3부 자동 개방 | PASS |
| 5 | 지목 카드 정확히 1장, 엠버 | PASS |
| 6 | 잠금 사유 선행명 포함 | PASS — `선행: 재의 수문` / `선행: 불씨 요새` |
| 7 | 사유가 흐려지지 않음 (감쇠 밖) | PASS |
| 8 | 두부 0 | PASS — 신규 문자열 전부 정상 |
| 9 | 라벨 겹침/넘침 0 | PASS — rect 감사 + 육안 |
| 10 | 탭 배지 최저비용 규칙 | PASS |
| 11 | 각인 컨텍스트 분화 | PASS |
| 12 | 훈련 폴드에서 시련 행 노출 | PASS — 0행 → **3행** 완전 가시 |
| 13 | 폰 390×844 스택 | PASS — 유효폭 798.7u, SORTIE 전체 무스크롤 |

**미실시**: 실제 플레이 세션(G8 인상 점수, G4 전투 연출).

---

# v1.8 — 인게임 안내 (run-id 20260807-ingame-guidance, AMENDMENT #9)

측정 2026-08-07. EditMode 최종 `engineering/unity-logs/test-results-164424.xml`
(**345/345, 실패 0**). WebGL `build-164527.log` (errors 0, 38 MB).
브라우저 증거 `qa/guidance-smoke/*.png` (10장).

**성격**: 순수 뷰 + 세이브 int 1개. 골든 무이동이 전제.

## g1 — 세계관 일관성

- 신규 문자열 전수: 법정 어휘 유지, 신규 고유명사 **0**.
  카탈로그 23종 Title/Body/TouchBody + 이탈 모달 4문안.
- 폰트 **532 글리프 coverage FULL** (500 → 532, `gen_hud_font.sh`).
- 신규 커버리지 T8이 **런타임 보간 조립 문자열**을 전수 `HasCharacter`한다 —
  기존 `FontCoverageTests`의 정규식 소스 스캔이 구조적으로 못 보는 차집합
  (`$"{VentPeriod:0.#}초마다"`의 숫자 부분 등).

**판정: PASS**

## g2 / g3 — 밸런스 · 유형

수치 미접촉. 이월.

## g4 — 이펙트 · 몰입 (겹침 4건 해소)

QA D2 유형 전수 감사가 추가 겹침 **8건**(101,792u², D2의 19.6배)을 찾았고,
안내가 쓸 좌표대와 겹치는 것 + D2 자체를 닫았다 (entry 15).

| # | 겹침 | 이전 | 이후 |
|---|---|---|---|
| A | 레벨업 토스트 × 성장 판 | 14,960u² (100% 매장) | **0** |
| B1 | 성장 판 × 스킬행 (터치 가로 = 권장 구성) | 27,528u² (81.6% 매장) | **0** |
| D | 위기/기세 × 웨이브 배너 | 9,000u² (완전 포함) | **0** |
| **D2** | **사망 패널 캠페인으로 × 사망사유** | **5,200u²** | **0** |
| + | 성장 판 제목이 판 밖 16u | 원본 결함 | 판 안 |

- 4구성(Phone/Full × desktop/touch) 전수 산술 겹침 **0**.
- 신규 표면 3종(정지 카드·이탈 모달·사망 패널) rect 감사 T7이 겹침 0 +
  바디 내부 격납을 고정.
- 대비: 정지 카드 hint InkDim **8.69:1**, 이탈 모달 제목 엠버 **6.11:1**.

**D2가 사용자 제보의 정체다** — 버튼은 작동했고(브라우저에서 클릭 성공,
유물 12→13 반영) 화면이 26u 겹침으로 숨겼다.

**판정: PASS** (전투 연출은 여전히 범위 밖 · 이월)

## g5 — 경제

- 이탈 정산 실측: 유물 **5 → 5** (증가 0), `clearedMask` **0 → 0**.
  `qa/guidance-smoke/08-abandon-forfeit-lobby.png`.
- 전량 몰수는 장르 소수파(2/16)이나 근거가 국지적이다 — 패배도 유물을
  적립하므로(`GameDirector.cs:614-626`) 등급 몰수면 "줍고 나가기"가
  죽을 위험 없는 최적 파밍이 된다 (entry 14).
- 모달이 손실 수치를 **명시**한다 (StS 실패 사례 회피).

**판정: PASS**

## g6 — 운영

- EditMode **345/345**, 실패 0. 320 → 345 (신규 25, 감소 0).
- 뮤테이션 검정 **17/17 검출**, 위양성 0. M12는 1차 생존 후 어서션을
  강화해 재검정에서 검출 — **통과하는 테스트를 지키는 테스트로 바꾼 사례**.
- WebGL errors 0, **38 MB** (한도 120).
- 브라우저 콘솔 에러 **0** (프롤로그·던전·시련·사망·이탈 전 경로).
- **timeScale 0 하드 프리즈 방어 확인**: 정지 중 체력 crop 바이트 4초 동일.
  `ResetRunUi`가 모든 종료 경로에서 카드를 뜯고 콜백은 발화 안 함.

**판정: PASS**

## g7 — 코어 루프

안내는 루프를 만들지 않고 **루프에 진입 가능하게** 한다.

- 커버리지 **2/23 (8.7%) → 23/23 (100%)**. 카탈로그 전수, 누락 0 (T1).
- 중복 0: `MarkSeen` 첫 호출만 true, 23비트 전수 라운드트립 (T2/T5).
- 정지 예산 실측 **정확히 8** (Hazard 6 + Outcome 2). 9번째는 T1이 잡는다.
- 최장 정지 카드 **15어절** (상한 33, `흑요석 기둥`).
- 스테이지 실측: cinder-sluice 진입 시 해류·분출구·흑요석 기둥 **3종 전부**
  큐잉 + 조작 토스트 + 패배 레슨 = 11/23. 기대와 일치.
- **런 중 이탈 경로 신설**: 0 → 1 (확인 모달 경유).

**판정: PASS**

## g8 — 참신성

| 후보 | 빈도 | 인상 |
|---|---|---|
| **G6 해저드 전용 안내** | **0/7** | 구조 확인 (스모크 04·09) |
| G9 사망 피드백 | 1/17 | 미측정 |
| G11 이탈 확인 모달 | 1/5 | 구조 확인 (스모크 07) |
| G12 스킵 (비트마스크 대체) | 1/8 | 구조 확인 (T2) |

G6이 최강이다. 장르 0/7은 회피가 아니라 **필요가 없었기** 때문 —
Gungeon·Hades·Dead Cells의 해저드는 가시·산성처럼 시각으로 자명해서
Gungeon은 "환경 요소는 엔티티가 아니다"로 문서화를 명시 제외했다.
우리 방벽주(적을 보호)·재의 장벽(주기 이동)·해류(밀기 200 vs 걸음 218)는
규칙이 외형으로 환원되지 않는다.

**인상 점수는 여전히 미측정 — 3사이클 연속 같은 지점.**

**판정: 빈도 PASS / 인상 미측정 → 조건부**

## 브라우저 스모크 (10항목)

| # | 항목 | 결과 |
|---|---|---|
| 1 | 첫 로드 + 콘솔 에러 | PASS — 0건 |
| 2 | 정지 카드 렌더 (kicker/title/body/hint) | PASS |
| 3 | 카드 문안이 심 수치 표시 | PASS — 2.4/0.8/90/8 |
| 4 | 아무 키로 해제 | PASS |
| 5 | 정지 중 체력 불변 | PASS — 4초 crop 동일 |
| 6 | 토스트 비정지 렌더 | PASS |
| 7 | 큐가 한 번에 한 장 | PASS |
| 8 | `guidanceSeen` 영속 | PASS — 0 → 1138815 |
| 9 | 사망 패널 D2 해소 | PASS |
| 10 | 이탈 모달 + 몰수 + 로비 복귀 | PASS |

**미실시**: 사람 플레이 세션 (G8 인상, G4 전투 연출), 모바일 실기기.

---

## v1.8 추가 — 사용자 제보 2건 (게이트 판정 후 발견)

게이트 v18을 쓴 뒤 사용자가 라이브에서 두 건을 제보했다. **둘 다 재현됐고
둘 다 게이트를 통과한 상태였다.** 이것이 이 절의 존재 이유다.

### 결함 9 — 모든 게이지가 `fillAmount`를 무시하고 있었다 `[OBSERVED]`

제보: "현재 게이지가 안 달고있는데"

실측: 프롤로그 사망 화면에서 **체력 0인데 바가 98% 채워짐**
(`11-prologue-trap-BEFORE.png`, y=14 행 밝은 픽셀 315/320).

원인: `Image.Type.Filled`인데 **`sprite`가 null**이다. uGUI에서 sprite가 없는
Image는 `Image.OnPopulateMesh`가 `Graphic`의 것으로 폴백해 **풀 렉트 하나를
그리고 `type`·`fillAmount`를 아예 읽지 않는다.** `Sync()`는 매 프레임 정확한
값을 쓰고 있었고, 아무도 그 값을 읽지 않았다.

**전수 감사: 7개 중 6개가 결함.**

| 게이지 | 상태 |
|---|---|
| `_stageClearFlash` | ok — `Overlay()`가 sprite를 넣는다 |
| `_chargeGaugeFill` 차지 | **DEFECT** |
| `_xpFill` 경험치 | **DEFECT** |
| `_bossFill` 보스 HP | **DEFECT** |
| `_extractRing` 추출 | **DEFECT** |
| `Bar()` 체력·기름 | **DEFECT** (2개 인스턴스) |
| `cooldownOverlay` 스킬 쿨다운 | **DEFECT** (5개 인스턴스) |

조치: 1×1 흰 sprite를 공유하는 `AsFilled()` 헬퍼로 6곳 전부 라우팅.

검증 [OBSERVED]: 체력 0 → 채움 **0%** (라벨 픽셀만), 기름 100 → **99%**.
수정 전 98%/98%. `13-gauges-fixed.png`.

**왜 기존 테스트가 못 잡았나**: `HudLayoutTests`·`LobbyLayoutTests`는 rect
크기와 겹침을 잰다. `fillAmount`가 **렌더에 반영되는지**는 어느 테스트의
정의역에도 없다. EditMode에서 `fill.fillAmount`를 읽으면 정확한 값이 나온다 —
필드는 맞고 렌더만 틀렸다.

### 결함 10 — 프롤로그에 나가는 길이 아예 없었다 `[OBSERVED]`

제보: "재점화만 있고 캠페인 돌아가기 없어. 전투에서"

실측: 프롤로그 사망 패널에 버튼이 **`재강하 (R)` 하나뿐**
(`11-prologue-trap-BEFORE.png`). 런 중에도 `포기` 버튼 없음.

원인 2겹:
1. `캠페인으로`를 `EnableCampaignUi()`가 붙이는데, 그 메서드는
   `_dungeonPresentation`일 때만 호출된다 (`GameView.cs:170`). 프롤로그는
   해당 없음 → **버튼이 애초에 생성되지 않는다.**
2. 내가 `SetAbandonAvailable`을 `_state == State.Dungeon`으로 게이팅했다.
   사유로 "프롤로그는 3웨이브면 스스로 끝난다"고 주석에 썼는데,
   **그건 이기는 경로에만 참이다.** 죽으면 재강하 루프에 갇힌다.

조치:
- 게임오버 패널의 `캠페인으로`를 `Build()`로 이동 — 모든 모드가 갖는다
- `SetAbandonAvailable`을 Dungeon/Prologue/Training/Arena 전부로
- `AbandonRun`의 상태 가드를 `!= Lobby`로

검증 [OBSERVED]: 프롤로그 사망 → `캠페인으로` + `재강하 (R)` 두 버튼
(`12-prologue-exit-AFTER.png`), 클릭 → 로비 도달
(`14-prologue-escaped-to-lobby.png`).

### 이 두 건이 게이트를 통과한 이유

| | 왜 통과했나 |
|---|---|
| 게이지 | 브라우저 스모크를 **던전에서만** 돌렸고, 던전 진입 시 체력 100이라 "꽉 찬 바"가 정상으로 보였다. 체력이 줄어드는 것을 **끝까지 본 적이 없다** |
| 프롤로그 출구 | 스모크 13항목이 전부 **던전 경로**였다. 프롤로그·아레나·시련은 한 번도 안 열었다 |

**공통 원인: 스모크 경로가 한 모드에 편중됐다.** 커버리지를 23종으로 넓히면서
검증 경로는 넓히지 않았다.

EditMode 345/345 유지. WebGL errors 0.

### 4모드 전수 스모크 (편중 해소)

결함 9·10의 공통 원인이 "스모크 경로가 던전에 편중"이었으므로, 네 실행 모드를
전부 열었다 `[OBSERVED]`.

| 모드 | 체력 바 | 기름 바 | `포기` 버튼 | 콘솔 |
|---|---|---|---|---|
| Dungeon | 반응 | 반응 | 있음 | 0 |
| Prologue | **0% @ 체력 0** | 99% | 있음 | 0 |
| Arena | 10% | 99% | **있음** | 0 |
| Training | 99% (무피격) | 99% | **있음** | 0 |

증거: `15-arena-mode.png`, `16-trial-mode.png`.
수정 전 아레나·시련은 **한 번도 열어본 적이 없었다.**

---

## v1.9 — AMENDMENT #9 코덱스 (run-id 20260807-ingame-guidance, cycle 6)

측정: EditMode `test-results-003311.xml`, 브라우저 `qa/codex-smoke/`.

### AC 17건 실측

| AC | 요구 | 실측 | 근거 |
|---|---|---|---|
| 1 | 스택이 모든 점유자와 0u | **0u** (5구성 × 3점유자) | `LeftStack_BothButtonsClearEveryOccupant` |
| 2 | 기존 결함 3건 해소 | 22u/32u/34u **RED→GREEN** | `LeftColumn_*` 3건 + RED 베이스라인 |
| 3 | 폰 버튼 ≥44 CSS px | 92u → **44.9px** | `AssertTouchFloor` |
| 4 | 조이스틱 겹침 0 | 랜드스케이프 **+132.6u / +92.9u** | 가로 배치 산술 |
| 5 | 로비에서 비활성 | `SetHudVisible(false)` → 스택 내림 | `SetLeftStackAvailable(false)` |
| 6 | 파생 5개 노출, 골든 불변 | 15개 노출, **골든 7건 통과** | `IDerivedStatSnapshot` |
| 7 | 4행 아이콘+값+분해 | 4행 렌더 | `01-stats-unfolded.png` |
| 8 | 계수 보간, 재입력 0 | 인자 재구성 = 심 값 ±1e-3 | `PrintedFactorsRecompose` |
| 9 | 레벨1에서 기본항만 | 문자열 동등 | `CollapseToTheBaseTerm` |
| 10 | 열린 프레임 동결 | Sync 2회 후 문자열 불변 | `LatchTheOpeningFrame` |
| 11 | 23항목 5그룹 | 23/23, 3열, **패널 안** | `EveryRowFitsInsideThePanel` |
| 12 | 미공개는 제목만 | 본문 부재 확인 | `UnseenEntriesLeakNoBody` |
| 13 | 공개는 `BodyFor(touch)` | 두 입력 모드 | `SeenEntriesShowTheBody` |
| 14 | 그룹 아이콘 | 4행 sprite 해결 또는 Image 비활성 | `CodexStatIcons` |
| 15 | timeScale 0 | **3초 간격 두 프레임 바이트 동일** | 브라우저 |
| 16 | 345/345 + 신규, errors 0 | **363/363**, WebGL 38MB errors 0 | 스위트 |
| 17 | 4모드 열림 | Dungeon/Prologue/Arena/Training **전부** | `03-05.png` |

### 4모드가 서로 다른 값을 보여준다 `[OBSERVED]`

| 모드 | 공격력 | 분해 | 왜 |
|---|---|---|---|
| Dungeon | 72.8 | `58 × 특성4(+12%) × 무기2(+12%)` | 메타·장비 적용 |
| Training | 72.8 | 동일 | `CinderSim.cs:2626` — "내 수치로 연습" |
| Prologue | 58.0 | `58` | 스톡 |
| Arena | 58.0 | `58` | 스톡 |

검산: 58×1.12×1.12=72.8 ✓ · 100+24+8=132 ✓ · 218×1.04=227 ✓ · 7×1.08=7.6 ✓

### 브라우저가 잡은 결함 3건 — 전부 EditMode 통과 중이었다

| # | 결함 | 테스트가 놓친 이유 |
|---|---|---|
| 11 | **분해가 `72.8 ← 72.8`** | AC-9 테스트가 `new CinderSim()`(아레나)를 씀. 메타·장비 0이라 `BaseDamage == SimConfig.PlayerDamage`가 되어 **캠페인 경로에서 공허하게 통과** |
| 12 | **안내 탭 238u 넘침** — 마지막 두 그룹 잘림 | 코덱스 테스트 9건이 전부 행이 **뭐라고 하는지**만 물었고 **어디 있는지**는 아무도 안 물음 |
| 13 | **사망 패널 위에 `포기` 살아 있음** | 모든 코덱스 테스트가 런 중에 열고 **런이 끝나면 어떻게 되는지**를 안 물음 |

세 건 다 대응 테스트를 추가했고, **12번은 변이로 증명**했다: 2열로 되돌리면
`승리 조건`·`패배 조건`이 패널 밖으로 나가며 RED.

11번이 §5b·§4i 계열이다 — 아레나 심에서는 두 값이 우연히 같아서 테스트가
구분하지 못했다. **"통과했다"와 "구분할 수 있다"는 다르다.**
