# Stage 1 Gate Review — v1.6 훈련장·돌발 (run-id 20260806-training-and-upgrade)

판정자: game-production-director. 규칙: 측정값 + 방법 + 증거 경로 3종 필수.
측정 원본: `qa/gate-measurements.md` §v1.6.

## G1 — 세계관 정합 (판정: PASS)

- 신규 가시 문자열 전수 감사 [OBSERVED, python cmap 대조]:
  시련 5종(불씨/해류/방벽/행진/증언 — 전부 기존 기믹 어휘를 그대로 승계),
  등급 3단(견습/숙련/판결 — **판결**은 법정 어휘 상위, T5 무기명 `판결인`과
  같은 계보), 버튼 `수련`, 상태 `미도전`/`최고 N`/`숙달 보상 수령됨`,
  HUD `위기`/`기세`. 새 고유명사 0개 — 전부 기존 법정 어휘 조합.
- 폰트 커버리지: 재생성 **490 → 497 글리프, v1.6 문자열 MISSING 0**
  [OBSERVED, `tools/gen_hud_font.sh`].
- **감사가 결함을 잡았다**: 최초 문자열에 U+00B7(·) 포함 — 소스 폰트 부재
  문자(v1.5에서 이미 기록된 함정)를 그대로 반복했다. U+2022(•)로 교체.
  같은 실수를 두 사이클 연속으로 했다는 사실은 회고에 남긴다.

## G2 — 규칙·밸런스 (판정: PASS)

- 위기 조항 회피량 **54.0 = 24.5% maxHP(220)**, 밴드 ≤30% 통과
  [OBSERVED, 생존 락스텝 20초 노출].
- director 사전 산술 25%와 실측 24.5% 일치 — 협상(entry 8)에서 6초 면제안을
  기각하고 3초×절반으로 강등한 판단이 실측으로 확인됐다.
- 역류인·증언인은 직접 피해 회피 0 — 밴드 무관 [OBSERVED].
- 단일 히트 상한: 신규 히트 소스 0개(돌발은 타이밍/배수만 건드림).

## G3 — 플레이어 유형 (판정: PASS)

- rusher 14킬 1기세(33.3s) / kiter 28킬 2기세(25.0s) [OBSERVED].
  둘 다 돌발 문에 도달, 유형 봉쇄 없음.

## G5 — 경제 (판정: PASS)

- 훈련장 15런(5시련×3등급) 전수 **유물 0 · 처치 0** [OBSERVED].
  적 스폰이 없어 드롭 경로가 구조적으로 부재 — PM 밴드("반복 통화 금지")를
  정책이 아니라 구조로 충족한다.
- 숙달 보상 +2는 1회성, 전 시련 판결 등급 게이트 (negotiation entry 7 서명).

## G6-ops — 운영 준비 (판정: PASS)

- WebGL **57.5MB · errors 0 · warnings 0 · 55.2s** [OBSERVED, build-142606.log].
  120MB 상한의 48%.
- 세이브 스키마: v5 필드 2개(`trialTiers`, `trainingMastery`) 순수 가산.
  구 블롭은 미싱키=0 규칙으로 "미도전/미수령"으로 로드 — v4 각인과 동일 문법.

## G7 — 코어 루프 (판정: **조건부 PASS**)

- 첫 기세 33.3~58.3s — 루프 대역 30-180s **안** [OBSERVED].
- **조건**: 4스테이지 중 2곳(ember-bastion, ash-march)은 프로브 파일럿이
  12킬 전에 죽어 기세를 못 봤다. 봇 실력 한계와 설계 문제를 **구분할 근거가
  아직 없다.** 사람 플레이 1회로 판별 가능하며 그때까지 PASS를 조건부로 둔다.
- 이 조건은 **웨이버가 아니다** — 다음 사이클 Stage 1a 진입 조건으로 이월한다.

## G8 — 참신성 (판정: **PASS(빈도) / 미확정(인상)**)

- 빈도: T8 기믹 전용 시련 **0/11**, S8 누적 처치 트리거 **0~1/13**
  [design/novelty-scorecard.md, 표본 ≥5 요건 충족].
- 자진 탈락 4건 기록 — 형식상 ≤2를 통과하지만 계열 포화·현상 유지·원형 충돌로
  designer가 스스로 내렸다. **이 자기검열이 이번 사이클 G8의 실질 산출이다.**
- 인상 점수(≥4/5)는 사람 플레이 필요 — 미측정, 따라서 **G8 최종 PASS 아님**.

## G4 — 성능 (판정: 이월 PASS)

- 신규 per-frame 비용: HUD 텍스트 2개(변경 시에만 재기록), 심 필드 읽기 2개.
  새 할당·새 렌더 패스 0. 16.7ms 예산에 유의미한 영향 없음 [INFERENCE —
  프레임 실측 미실시, 따라서 게이트 인용 불가로 표기].

---

## 종합 판정

**PASS 6 (G1·G2·G3·G5·G6·G4이월) / 조건부 PASS 1 (G7) / 부분 1 (G8)**

머지 가능. 두 미결은 **사람 플레이 1세션**으로 동시에 해소된다:
G7 기세 도달률, G8 인상 점수.

## 이 사이클이 실제로 증명한 것

게이트가 결함 4건을 잡았고 **사람은 0건 잡았다**:

| 잡은 주체 | 결함 |
|---|---|
| Editor.log | 컴파일 2종 — 플레이 불가 상태 |
| EditMode 8건 + 골든 1행 | `_trainingRate` 미초기화 → 전 기믹 시계 정지 |
| LobbyLayoutTests 래칫 | 신규 최소 터치 타겟 28.3px |
| dotnet 프로브 | 멀티킬 틱이 기세 임계 건너뜀 |

특히 4번은 **스펙에 "두 문 모두 임계 건너뜀 금지"라고 써놓고 한쪽만 구현한**
경우다. 문서가 옳았고 구현이 문서를 안 읽었다. 래칫이 없었다면 넷 다 배포됐다.
